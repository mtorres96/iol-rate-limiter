---
phase: 05-quality-swagger-compliance
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - rate-limiter/COMPLIANCE.md
  - rate-limiter/README.md
  - rate-limiter/DESIGN.md
  - rate-limiter/src/store/memory.ts
  - rate-limiter/src/store/redis.ts
  - rate-limiter/src/adapters/express/middleware.ts
autonomous: true
requirements: [D-09, D-10, D-11, D-12, "AF-3", "DELIV-04", "DELIV-06"]
user_setup: []

must_haves:
  truths:
    - "COMPLIANCE.md maps every brief 'Focus on' MUST and every 'Nice to have' to concrete repo evidence (file/test/section) with a status"
    - "COMPLIANCE.md honestly marks logging/metrics (OBS-01/02) as v2-deferred, not delivered"
    - "COMPLIANCE.md records the audit dispositions for AF-1/AF-2/AF-3"
    - "README states the coverage number and links to /docs; DESIGN.md notes the Swagger decision and the audit's material fixes"
    - "Targeted doc-comments clarify non-obvious intent only (no comment-slop on trivial code)"
    - "npm run verify still exits 0 after all changes"
  artifacts:
    - path: "rate-limiter/COMPLIANCE.md"
      provides: "Brief→evidence mapping table + audit dispositions (D-10)"
      contains: "Focus on"
      min_lines: 40
    - path: "rate-limiter/README.md"
      provides: "Coverage statement + /docs link (D-12)"
      contains: "/docs"
    - path: "rate-limiter/DESIGN.md"
      provides: "Swagger decision + audit material-fixes note (D-12)"
      contains: "Swagger"
  key_links:
    - from: "rate-limiter/COMPLIANCE.md"
      to: "repo evidence (files/tests/doc sections)"
      via: "brief item → evidence table rows"
      pattern: "\\.test\\.ts|src/|DESIGN.md"
---

<objective>
Run the skill-assisted re-audit, apply only material findings, add the COMPLIANCE.md brief→evidence map, do the targeted non-obvious doc-comment top-up, and update README + DESIGN.md to reflect the hardened state — so the documents a grader opens first show the final coverage gate, Swagger docs, and honest scope.

Purpose: D-09 (fix material only, record the rest), D-10 (COMPLIANCE.md), D-11 (targeted comments), D-12 (README/DESIGN updates). This closes the grading-hardening pass. The gap audit is confirmatory — the brief PDF is identical to PROJECT.md; COMPLIANCE.md confirms compliance, it does not add scope.
Output: rate-limiter/COMPLIANCE.md, updated README.md + DESIGN.md, and targeted comment upgrades on non-obvious source intent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-swagger-compliance/05-CONTEXT.md
@.planning/phases/05-quality-swagger-compliance/05-RESEARCH.md
@.planning/phases/05-quality-swagger-compliance/05-PATTERNS.md
@rate-limiter/DESIGN.md
@rate-limiter/README.md

<interfaces>
<!-- COMPLIANCE.md target rows are fully enumerated in 05-RESEARCH §"COMPLIANCE.md Target Map". -->
<!-- The brief PDF (iol-challenge-actualizado (1).pdf, Version 89f729a) is confirmed identical to PROJECT.md. -->

Audit findings already surfaced (05-RESEARCH §"Audit Findings"):
- AF-1: eslint unused-var at middleware.test.ts:99 — FIXED in Plan 01.
- AF-2: 2 stale eslint-disable directives in server.ts — FIXED in Plan 01.
- AF-3: lint not part of verify — DECISION to record here. D-03 names only typecheck+coverage; since Plan 01 fixed AF-1/AF-2, `eslint .` now exits 0, so adding lint to verify is now low-risk. Make the call and record the rationale in COMPLIANCE.md/DESIGN.md either way.

Honesty rule (load-bearing): logging/metrics are v2-deferred (OBS-01/02). COMPLIANCE.md maps them as "deferred (rationale)", NEVER as delivered. The DegradedLogger hook in redis.ts/middleware.ts is the seam, not a delivered feature.

D-11 comment targets (non-obvious intent ONLY — leave trivial getters/barrels alone):
- Lua atomicity contracts (analog: src/store/lua/token-bucket.lua L1-15)
- sliding-window math + the memory.ts:167 prev>0/prev===0 branch (note its reachability if a /* v8 ignore */ landed in Plan 01)
- fixed-window boundary-burst
- breaker fail-open-vs-closed policy (src/store/redis.ts / breaker.ts)
- OpTuple rounding parity
Comment-style bar to match: src/validate.ts L9-14 / L21-32 (intent-first, "what corruption this prevents"). Do NOT restate code.

DESIGN.md insertion points (05-PATTERNS): near `## 7. The npm run verify gate` (L200) and `## 9. Scope note` (L261).
README insertion points: near `## Try it: a 200, then a 429` (L35) and `## Verify` (L104).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skill-assisted audit + targeted non-obvious doc-comments + AF-3 lint-in-verify decision</name>
  <files>rate-limiter/src/store/memory.ts, rate-limiter/src/store/redis.ts, rate-limiter/src/adapters/express/middleware.ts</files>
  <read_first>
    - rate-limiter/src/store/memory.ts (L150-172 — sliding-window retryAfterMs math, the L167 branch)
    - rate-limiter/src/store/redis.ts (breaker / fail-open-vs-closed policy region)
    - rate-limiter/src/adapters/express/middleware.ts (never-leak try/catch, fail-open/closed)
    - rate-limiter/src/store/lua/token-bucket.lua (L1-15 — the parity-contract comment bar to match)
    - rate-limiter/src/validate.ts (L9-14, L21-32 — intent-comment style)
    - .planning/phases/05-quality-swagger-compliance/05-RESEARCH.md §"Audit Findings" + §"Open Questions" (AF-3 lint-in-verify)
    - .planning/phases/05-quality-swagger-compliance/05-PATTERNS.md "Targeted doc-comments (D-11)" section
  </read_first>
  <action>
    Apply the skill-assisted audit lenses (code-quality / design-patterns / docker-expert / documentation-writer — discretion on order; load any matching .claude/skills SKILL.md if present, else proceed). D-09: apply ONLY material findings (correctness, security, clear APOSD/readability wins) in their offending file; everything stylistic/speculative is recorded (in Task 2's COMPLIANCE.md), NOT acted on. Do NOT refactor working, tested code days before submission.
    D-11 targeted comment top-up — add/upgrade comments ONLY where intent is non-obvious, matching the validate.ts/lua intent-first style: sliding-window math in memory.ts (and, if Plan 01 left a `/* v8 ignore … @preserve */` at memory.ts:167, ensure its one-line reachability justification reads clearly), the breaker fail-open-vs-closed policy in redis.ts, the never-leak fail-open/closed try/catch in middleware.ts, fixed-window boundary-burst, OpTuple rounding parity. Leave trivial getters/barrels untouched (the brief penalizes comment-slop). Tag new comments with the governing decision ID where one exists.
    AF-3 decision: `eslint .` now exits 0 (Plan 01 fixed AF-1/AF-2). Decide whether to add `lint` to the `verify` script. If yes, update package.json `verify` to include `&& npm run lint` AFTER the coverage run. Record the decision + rationale (it lands in COMPLIANCE.md/DESIGN.md in Tasks 2/3) regardless of which way you go.
  </action>
  <verify>
    <automated>cd rate-limiter && npm run verify && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `cd rate-limiter && npm run verify` exits 0 (coverage gate still green after comment/source touches).
    - `cd rate-limiter && npx eslint .` exits 0.
    - Comment changes are confined to non-obvious-intent regions (sliding-window math, breaker policy, fail-open/closed, boundary-burst, OpTuple parity); no comments added to trivial getters/barrels (diff review).
    - The AF-3 lint-in-verify decision is made; if "yes", package.json `verify` includes `npm run lint`; either way the rationale is captured for COMPLIANCE.md/DESIGN.md.
    - No behavior-changing refactor of working tested code (coverage numbers unchanged or improved, all tests still pass).
  </acceptance_criteria>
  <done>Material findings fixed, non-obvious intent comments added (no slop), AF-3 decision recorded, and the green gate remains green.</done>
</task>

<task type="auto">
  <name>Task 2: Author COMPLIANCE.md (brief→evidence map + audit dispositions)</name>
  <files>rate-limiter/COMPLIANCE.md</files>
  <read_first>
    - rate-limiter/DESIGN.md (voice/structure: numbered `## N. Title`, honest evidence-anchored tone, §8 AI-usage, §9 scope note)
    - .planning/PROJECT.md (the brief's Rules / Focus-on / Nice-to-haves — identical to the PDF Version 89f729a)
    - .planning/REQUIREMENTS.md (v1 IDs + the v2-deferred OBS-01/02)
    - .planning/phases/05-quality-swagger-compliance/05-RESEARCH.md §"COMPLIANCE.md Target Map" (the full brief→evidence rows) + §"Audit Findings"
    - .planning/phases/05-quality-swagger-compliance/05-PATTERNS.md "rate-limiter/COMPLIANCE.md" section
  </read_first>
  <action>
    Create rate-limiter/COMPLIANCE.md (alongside DESIGN.md/README.md, inside the submission folder — D-10). Match DESIGN.md's voice. Build three scannable tables — Rules (MUST), Focus on, Nice to haves — each row `brief item → evidence (file/test/§) → status`, using the rows enumerated in 05-RESEARCH §"COMPLIANCE.md Target Map" with the ACTUAL final evidence (the new ≥95% coverage gate, the /docs Swagger UI, the specific test files). Add an Audit Dispositions table seeded from AF-1 (fixed, Plan 01), AF-2 (fixed, Plan 01), AF-3 (the lint-in-verify decision from Task 1 + rationale), plus any further material findings from Task 1 and the recorded-not-acted stylistic findings.
    HONESTY RULE: map logging/metrics (OBS-01/02) as "deferred (rationale: v2 per PROJECT.md; DegradedLogger hook is the seam)", NEVER as delivered. State the PDF is confirmed identical to PROJECT.md (Version 89f729a) so the gap audit is confirmatory. Note that COMPLIANCE.md reflects the FINAL state (generated after the audit fixes — D-09 discretion).
  </action>
  <verify>
    <automated>cd rate-limiter && test -f COMPLIANCE.md && grep -q "deferred" COMPLIANCE.md && grep -qi "Focus on" COMPLIANCE.md && grep -q "89f729a" COMPLIANCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - rate-limiter/COMPLIANCE.md exists with a row for every "Focus on" MUST and every "Nice to have" from the brief, each mapping to a file/test/doc-section evidence anchor and a status.
    - Logging/metrics rows are marked "deferred" with a rationale — NOT claimed as delivered (grep for "deferred" succeeds; no row claims pino/prom-client shipped).
    - An Audit Dispositions table records AF-1/AF-2 (fixed) and AF-3 (decision + rationale).
    - The file notes the PDF Version 89f729a is identical to PROJECT.md (gap audit confirmatory).
    - Evidence anchors point at real paths (e.g. `test/token-bucket.test.ts`, `src/store/redis.ts`, the `/docs` route, the coverage gate).
  </acceptance_criteria>
  <done>COMPLIANCE.md is a scannable, honest brief→evidence map with audit dispositions, reflecting the final hardened state.</done>
</task>

<task type="auto">
  <name>Task 3: Update README + DESIGN.md to reflect the hardened state (coverage, /docs, Swagger decision, material fixes)</name>
  <files>rate-limiter/README.md, rate-limiter/DESIGN.md</files>
  <read_first>
    - rate-limiter/README.md (the `## Try it: a 200, then a 429` (L35) and `## Verify` (L104) sections — where a grader looks first)
    - rate-limiter/DESIGN.md (the `## 7. The npm run verify gate` (L200) and `## 9. Scope note` (L261) sections — matching style)
    - rate-limiter/COMPLIANCE.md (the artifact created in Task 2 — link to it)
    - .planning/phases/05-quality-swagger-compliance/05-RESEARCH.md (final coverage numbers; Swagger decision D-06)
    - .planning/phases/05-quality-swagger-compliance/05-PATTERNS.md "README.md / DESIGN.md" section
  </read_first>
  <action>
    README.md (D-12): add a coverage statement (the final ≥95% four-metric number from the green gate — state branches/lines/statements/functions) near `## Verify`, and a `/docs` link (how to reach Swagger UI on the running demo, e.g. http://localhost:PORT/docs) near `## Try it: a 200, then a 429`. Reference COMPLIANCE.md.
    DESIGN.md (D-12): add a short new numbered `##` section (matching its style, slotted near `## 7. The npm run verify gate` and `## 9. Scope note`) covering: the Swagger decision (D-06 hand-written typed OpenAPI object, no codegen, demo-scoped docs) and the audit's material fixes (AF-1/AF-2 lint fixes, the AF-3 lint-in-verify decision). Keep the honest, non-overstated voice; do not claim logging/metrics. Link COMPLIANCE.md.
    Do NOT inflate — state the actual coverage number and the actual scope. Re-run the gate to confirm nothing regressed.
  </action>
  <verify>
    <automated>cd rate-limiter && grep -qi "coverage" README.md && grep -q "/docs" README.md && grep -qi "swagger" DESIGN.md && npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - README.md states the final coverage number and links to /docs (grep for "coverage" and "/docs" both succeed).
    - DESIGN.md has a new section noting the Swagger decision and the audit's material fixes (grep for "swagger" succeeds, case-insensitive).
    - Both docs reference COMPLIANCE.md.
    - Neither doc claims logging/metrics as delivered.
    - `cd rate-limiter && npm run verify` exits 0 (no regression from doc/source touches).
  </acceptance_criteria>
  <done>README and DESIGN.md reflect the hardened state — coverage number, /docs link, Swagger decision, and material audit fixes — honestly and without overstatement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grader → docs | COMPLIANCE.md / README / DESIGN.md are read-only graded artifacts; the risk is overstatement (claiming unshipped features), not a runtime attack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Repudiation / Information Disclosure | COMPLIANCE.md / README / DESIGN.md overstating scope | mitigate | Honesty rule enforced: logging/metrics mapped as v2-deferred with rationale, never claimed delivered; every evidence anchor points at a real path; coverage number stated, not invented. |
| T-05-08 | Tampering | a "fix everything" audit regressing working tested code | mitigate | D-09 = fix material only, record the rest; D-11 = comment non-obvious intent only; `npm run verify` re-run after every task confirms no regression. |
| T-05-09 | (no new runtime surface) | docs/comments only | accept | This plan touches docs + comments + an optional verify-script line; no new code path, dependency, or network surface. No HIGH-severity threats. |
</threat_model>

<verification>
- `cd rate-limiter && npm run verify` exits 0 (gate green after all audit/doc changes).
- `cd rate-limiter && npx eslint .` exits 0.
- COMPLIANCE.md exists with brief→evidence tables + audit dispositions; logging/metrics marked deferred.
- README states coverage + /docs link; DESIGN.md notes Swagger + material fixes.
</verification>

<success_criteria>
- Material audit findings fixed; the rest recorded with rationale (D-09).
- COMPLIANCE.md maps every brief item to honest repo evidence (D-10).
- Targeted non-obvious comments added; no comment-slop (D-11).
- README + DESIGN.md reflect the hardened state; AF-3 decision recorded (D-12).
- Green gate remains green throughout.
</success_criteria>

<output>
Create `.planning/phases/05-quality-swagger-compliance/05-03-SUMMARY.md` when done. Record: the AF-3 lint-in-verify decision and rationale, the final coverage number stated in README, any additional material findings from the skill-assisted audit (and their disposition), and confirmation that COMPLIANCE.md marks logging/metrics as deferred (not delivered).
</output>
