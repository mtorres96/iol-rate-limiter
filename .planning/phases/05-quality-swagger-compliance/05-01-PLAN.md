---
phase: 05-quality-swagger-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rate-limiter/test/validate.test.ts
  - rate-limiter/test/redis-connect.test.ts
  - rate-limiter/test/sliding-window.test.ts   # memory.ts:167 covering test (only if branch is reachable)
  - rate-limiter/src/store/memory.ts            # ONLY if memory.ts:167 is unreachable — add justified /* v8 ignore … @preserve */
  - rate-limiter/test/adapters/express/middleware.test.ts
  - rate-limiter/vitest.config.ts
  - rate-limiter/package.json
  - rate-limiter/src/demo/server.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, "TEST-01", "DELIV-03", "AF-1", "AF-2"]
user_setup: []

must_haves:
  truths:
    - "cd rate-limiter && npm run verify exits 0 with coverage thresholds enforced"
    - "Coverage report shows branches/lines/statements/functions all >= 95 on the D-01 scope, enforced as a four-metric hard gate (D-02)"
    - "eslint . exits 0 (AF-1 unused-var and AF-2 stale directives fixed)"
    - "The validate.ts throw arms, RedisStore.connect(), headers.ts ietf/windowSeconds, and middleware.ts TypeError arm are exercised by a real test"
    - "The memory.ts:167 prev===0 else-branch is resolved before the threshold gate — real covering test preferred, justified /* v8 ignore … @preserve */ pragma only if genuinely unreachable, disposition recorded (D-04)"
  artifacts:
    - path: "rate-limiter/test/validate.test.ts"
      provides: "Coverage of the three validate.ts throw arms (assertPositiveConfig/assertPolicy/assertPrefix)"
      contains: "toThrow(RangeError)"
    - path: "rate-limiter/test/redis-connect.test.ts"
      provides: "No-Docker coverage of RedisStore.connect() static factory both branches"
      contains: "RedisStore.connect"
    - path: "rate-limiter/vitest.config.ts"
      provides: "Coverage exclude list (demo/barrels/lua) + four-metric thresholds"
      contains: "thresholds"
    - path: "rate-limiter/package.json"
      provides: "verify script runs coverage (D-03)"
      contains: "--coverage"
  key_links:
    - from: "rate-limiter/package.json"
      to: "vitest coverage thresholds"
      via: "verify script invoking vitest run --coverage"
      pattern: "verify.*coverage"
    - from: "rate-limiter/vitest.config.ts"
      to: "src/store/lua/**"
      via: "coverage.exclude (rolldown cannot parse .lua)"
      pattern: "lua"
---

<objective>
Drive the coverage gate green by closing the real uncovered branches with focused tests, fix the two confirmed lint findings (AF-1, AF-2), then enable the hard four-metric coverage threshold and wire it into `npm run verify` — IN THAT ORDER, so the gate is never enabled before the branches it gates are covered.

Purpose: The ≥95% coverage gate (D-01/D-02/D-03) is the mandatory green gate for grading; branches is the only metric currently below 95 (88.18%). All gaps are reachable defensive arms coverable with small no-source-change tests.
Output: New/extended test files closing every enumerated branch gap; AF-1/AF-2 lint fixes; vitest.config.ts thresholds + exclude list; package.json verify wired to coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-swagger-compliance/05-CONTEXT.md
@.planning/phases/05-quality-swagger-compliance/05-RESEARCH.md
@.planning/phases/05-quality-swagger-compliance/05-PATTERNS.md

<interfaces>
<!-- Uncovered branches with exact line numbers (from 05-RESEARCH Coverage Gap Analysis). -->
<!-- All reachable via construction/stub/supertest; NO production source changes needed for coverage. -->

src/validate.ts throw arms (currently uncovered):
- L17 assertPositiveConfig: throw new RangeError (only assertCost throw is tested today)
- L47 assertPolicy: throw new RangeError
- L60 assertPrefix: throw new RangeError

src/store/redis.ts L121-129: RedisStore.connect() static factory.
- Uses lazyConnect:true → constructing touches no network (no Docker needed).
- Branch at ~L128: `connection ? new Redis(connection) : new Redis(options)` — hit BOTH (with URL and no-arg).
- Release via `await store.close()`.

src/adapters/express/headers.ts L49-57:
- The `mode === 'ietf'`-only branch (only `legacy` and `false` tested today).
- The `windowSeconds != null ? … : ''` ternary (no test passes windowSeconds).

src/adapters/express/middleware.ts L68:
- `if (options.limiter == null) throw new TypeError(...)` arm.

src/store/memory.ts L167 (DO NOT touch in this plan unless trivially reachable):
- `prev > 0 ? cfg.windowMs / prev : msToBoundary` ELSE branch (prev === 0 fallback).
- Investigate reachability FIRST (D-04). If a crafted sliding-window state reaches it, add a test.
  If proven unreachable, add `/* v8 ignore next -- <reason: prev===0 unreachable given curr+cost>limit guard above> @preserve */` (the `@preserve` is MANDATORY — esbuild strips unmarked comments).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close validate.ts throw arms + RedisStore.connect() with no-Docker construction tests; resolve memory.ts:167 branch (test or justified pragma)</name>
  <files>rate-limiter/test/validate.test.ts, rate-limiter/test/redis-connect.test.ts</files>
  <read_first>
    - rate-limiter/src/validate.ts (the three throw arms at L17/47/60)
    - rate-limiter/src/store/redis.ts (L116-130 — connect() factory, lazyConnect:true)
    - rate-limiter/test/breaker.test.ts (construction-test pattern, FakeClock, no Docker)
    - rate-limiter/test/degraded.test.ts (rejectingClient() stub + close() release pattern)
    - rate-limiter/test/cost-validation.test.ts (assertCost throw ALREADY tested — do NOT duplicate)
    - rate-limiter/src/store/memory.ts (L150-172 — sliding-window retryAfterMs; the L167 `prev > 0 ? … : msToBoundary` else-branch)
    - rate-limiter/test/sliding-window.test.ts (where a covering test lands if L167 is reachable)
    - .planning/phases/05-quality-swagger-compliance/05-RESEARCH.md §"Coverage Gap Analysis" + §Pattern 2
    - .planning/phases/05-quality-swagger-compliance/05-PATTERNS.md "test/validate.test.ts" + "test/redis-connect.test.ts" sections
  </read_first>
  <behavior>
    - assertPositiveConfig throw arm: `new TokenBucketLimiter(new MemoryStore(), { capacity: 0, refillPerInterval: 1, intervalMs: 1 })` throws RangeError.
    - assertPolicy throw arm: `RedisStore.connect("redis://x", { policy: "nope" as never })` throws RangeError.
    - assertPrefix throw arm: `RedisStore.connect("redis://x", { keyPrefix: "" })` throws RangeError.
    - RedisStore.connect("redis://127.0.0.1:6379") returns a RedisStore instance without connecting; releases via await store.close().
    - RedisStore.connect() with no URL returns a RedisStore instance (hits the no-arg branch); releases via await store.close().
    - memory.ts:167 disposition: if reachable, a sliding-window test drives the `prev === 0` else-branch and asserts the resulting retryAfterMs; if unreachable, the branch is covered by a justified `/* v8 ignore next -- … @preserve */` so the branch metric is satisfied.
  </behavior>
  <action>
    Create rate-limiter/test/validate.test.ts covering the three throw arms (assertPositiveConfig via TokenBucketLimiter capacity:0; assertPolicy and assertPrefix via RedisStore.connect with bad policy / empty keyPrefix — lazyConnect:true means no network, no Docker). Import from "../src/index.js". Do NOT re-test assertCost (already in cost-validation.test.ts).
    Create rate-limiter/test/redis-connect.test.ts covering RedisStore.connect() both branches: with a "redis://..." URL and with no argument (default options). Assert `instanceof RedisStore`, then `await store.close()` in each to release the lazily-built ioredis client (mirror degraded.test.ts close()). These are no-Docker tests.
    Use the construction-test pattern from 05-PATTERNS.md verbatim for identifiers; assert `.toThrow(RangeError)`. Reference D-04 (real test before pragma) in a one-line file comment.
    memory.ts:167 (MANDATORY — must be resolved here, BEFORE the Task 3 threshold gate is enabled, or the 95% branch threshold can fail). Investigate the `prev > 0 ? cfg.windowMs / prev : msToBoundary` else-branch reachability FIRST (D-04 order). If REACHABLE — craft a burst sliding-window state where curr+cost>limit (request rejected) with zero prior-window requests so `prev === 0` — add a covering test in rate-limiter/test/sliding-window.test.ts (the in-memory/FakeClock sliding-window test file) asserting the else-branch retryAfterMs. If GENUINELY UNREACHABLE, add `/* v8 ignore next -- prev===0 unreachable given the curr+cost>limit guard above @preserve */` at memory.ts:167 (the `@preserve` is MANDATORY — esbuild strips unmarked comments). Record the chosen disposition (covering test vs justified pragma, with the justification) in this plan’s SUMMARY. This is the ONLY ignore-pragma candidate in the phase; do not pragma any other branch.
  </action>
  <verify>
    <automated>cd rate-limiter && npx vitest run test/validate.test.ts test/redis-connect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd rate-limiter && npx vitest run test/validate.test.ts test/redis-connect.test.ts` exits 0 with all new tests passing.
    - test/validate.test.ts contains three `toThrow(RangeError)` assertions for assertPositiveConfig/assertPolicy/assertPrefix and does NOT reference assertCost.
    - test/redis-connect.test.ts calls `RedisStore.connect(...)` with a URL AND with no argument, and calls `await store.close()` in both cases.
    - The memory.ts:167 `prev === 0` else-branch is resolved: EITHER a passing sliding-window test in test/sliding-window.test.ts drives it, OR memory.ts:167 carries a justified `/* v8 ignore next -- … @preserve */` pragma — and the chosen disposition (test vs pragma + justification) is recorded for the SUMMARY.
    - No production source file under src/ is modified by this task EXCEPT the single optional `/* v8 ignore … @preserve */` comment at memory.ts:167 in the unreachable case (no behavior change).
  </acceptance_criteria>
  <done>The three validate.ts throw arms and both RedisStore.connect() branches are exercised by passing no-Docker tests, and the memory.ts:167 else-branch is resolved (covering test or justified @preserve pragma) — all before the Task 3 threshold gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close headers.ts ietf/windowSeconds + middleware.ts TypeError arm; fix AF-1 and AF-2 lint findings</name>
  <files>rate-limiter/test/adapters/express/middleware.test.ts, rate-limiter/src/demo/server.ts</files>
  <read_first>
    - rate-limiter/test/adapters/express/middleware.test.ts (the file itself — existing supertest cases + the AF-1 unused `_d` at L99)
    - rate-limiter/src/adapters/express/headers.ts (L49-57 — ietf-only branch + windowSeconds ternary)
    - rate-limiter/src/adapters/express/middleware.ts (L67-69 — limiter==null TypeError arm)
    - rate-limiter/src/demo/server.ts (L130, L135 — the two stale `eslint-disable no-console` directives, AF-2)
    - rate-limiter/test/demo.test.ts (single keep-alive agent → stable req.ip pattern, L55)
    - .planning/phases/05-quality-swagger-compliance/05-RESEARCH.md §"Audit Findings Already Surfaced" (AF-1/AF-2)
    - .planning/phases/05-quality-swagger-compliance/05-PATTERNS.md "test/adapters/express/middleware.test.ts" section
  </read_first>
  <behavior>
    - A supertest with `rateLimit({ limiter, headers: 'ietf' })` exercises the ietf-only header branch and asserts a `RateLimit` header is emitted (and X-RateLimit-* are not, per ietf-only mode).
    - A supertest passing a `windowSeconds`-bearing policy exercises the `windowSeconds != null ? … : ''` ternary (RateLimit-Policy includes `;w=<windowSeconds>`).
    - `rateLimit({} as never)` throws TypeError (missing limiter arm).
  </behavior>
  <action>
    Extend rate-limiter/test/adapters/express/middleware.test.ts: add one supertest case driving `headers: 'ietf'` (assert the IETF `RateLimit` header present); add one case where the limiter/policy yields a `windowSeconds` so the `;w=` ternary branch (headers.ts L49) is taken; add `expect(() => rateLimit({} as never)).toThrow(TypeError)` for the middleware.ts L68 arm. Reuse the existing keep-alive single-agent supertest pattern in this file for stable req.ip.
    Fix AF-1: at middleware.test.ts:99 rename the unused `_d` binding to `_` (the established convention) so `@typescript-eslint/no-unused-vars` passes.
    Fix AF-2: in src/demo/server.ts remove the two stale `eslint-disable no-console` directives at L130 and L135 (they report "Unused eslint-disable directive"); if a startup console.log is intentional, keep the minimum necessary directive only.
  </action>
  <verify>
    <automated>cd rate-limiter && npx vitest run test/adapters/express/middleware.test.ts && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `cd rate-limiter && npx vitest run test/adapters/express/middleware.test.ts` exits 0 with the new ietf / windowSeconds / TypeError cases passing.
    - `cd rate-limiter && npx eslint .` exits 0 (AF-1 unused `_d` and AF-2 stale directives resolved).
    - middleware.test.ts contains a `headers: 'ietf'` case, a windowSeconds case, and `toThrow(TypeError)`.
    - The only src/ change is removing/trimming the stale eslint-disable directives in src/demo/server.ts (no behavior change).
  </acceptance_criteria>
  <done>headers.ts ietf-mode + windowSeconds branches and middleware.ts TypeError arm are covered; eslint . exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Enable the four-metric coverage gate + exclude list and wire verify to coverage</name>
  <files>rate-limiter/vitest.config.ts, rate-limiter/package.json</files>
  <read_first>
    - rate-limiter/vitest.config.ts (current coverage block — provider:'v8', include:['src/**'], NO exclude/thresholds)
    - rate-limiter/package.json (scripts: verify = typecheck && test; coverage = vitest run --coverage)
    - .planning/phases/05-quality-swagger-compliance/05-RESEARCH.md §"Recommended vitest.config.ts shape" + §"verify script wiring"
    - .planning/phases/05-quality-swagger-compliance/05-PATTERNS.md "vitest.config.ts" + "package.json" sections
  </read_first>
  <action>
    In rate-limiter/vitest.config.ts add to the existing `coverage` block (keep provider:'v8' and include:['src/**']; keep fileParallelism:false and the bumped timeouts UNTOUCHED): an `exclude` list of EXACTLY `src/demo/**`, `src/index.ts`, `src/adapters/express/index.ts`, `src/store/lua/**` (the `.lua` exclude is MANDATORY — rolldown throws PARSE_ERROR on Lua and the demo/barrels would crater the global gate); and `thresholds: { lines: 95, statements: 95, functions: 95, branches: 95 }` (D-02). Do NOT set perFile:true. Add one-line comments tagging D-01/D-02 on the exclude/thresholds.
    In rate-limiter/package.json wire D-03 single green gate: change `"verify"` to `"npm run typecheck && vitest run --coverage"` (Option A — minimal; keeps `test` as-is). Do NOT add `lint` to verify in this task (D-03 names only typecheck+coverage; the lint-in-verify decision is recorded in Plan 03).
    This task runs LAST in this plan: the branch-closing tests from Tasks 1-2 must already be green or the newly-enabled threshold fails the run.
  </action>
  <verify>
    <automated>cd rate-limiter && npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `cd rate-limiter && npm run verify` exits 0 (typecheck + coverage run with thresholds enforced).
    - The coverage report prints branches/lines/statements/functions all >= 95 for the D-01 scope.
    - vitest.config.ts coverage.exclude contains all four of: `src/demo/**`, `src/index.ts`, `src/adapters/express/index.ts`, `src/store/lua/**`.
    - vitest.config.ts coverage.thresholds sets lines/statements/functions/branches to 95.
    - package.json `verify` script invokes `vitest run --coverage`.
    - No PARSE_ERROR / "Failed to parse … .lua" lines appear in the coverage output.
  </acceptance_criteria>
  <done>The hard four-metric coverage gate is enabled, the .lua/demo/barrel excludes are in place, and `npm run verify` passes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → source | Tests construct limiters/stores with adversarial config (bad policy, empty prefix, capacity 0); no untrusted network input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | coverage gate (vitest.config thresholds) | mitigate | Gate enabled only AFTER branch tests land (Task 3 last); `.lua`/demo/barrel excludes prevent a silently-passing-but-meaningless gate; thresholds on all four metrics (D-02). |
| T-05-02 | Repudiation | scope-reduction to dodge a real branch | accept | D-01 fixes the exclude scope; this plan covers branches with real tests (D-04) rather than expanding excludes. No exclude beyond demo/barrels/lua. |
| T-05-03 | (no new runtime surface) | — | accept | This plan adds tests + config + a lint tidy only; no new code path, dependency, or network surface. No HIGH-severity threats. |
</threat_model>

<verification>
- `cd rate-limiter && npm run verify` exits 0 with four-metric thresholds enforced.
- `cd rate-limiter && npx eslint .` exits 0.
- No `.lua` PARSE_ERROR in coverage output; global branch coverage >= 95.
</verification>

<success_criteria>
- All enumerated branch gaps (validate throws, RedisStore.connect, headers ietf/windowSeconds, middleware TypeError) covered by passing tests.
- AF-1 and AF-2 lint findings fixed; `eslint .` clean.
- Coverage gate live on all four metrics; `npm run verify` green.
- Zero production-logic source changes beyond removing stale eslint-disable directives.
</success_criteria>

<output>
Create `.planning/phases/05-quality-swagger-compliance/05-01-SUMMARY.md` when done. Record: final coverage numbers per metric, whether memory.ts:167 was covered by a test or a justified `/* v8 ignore … @preserve */` pragma (and the justification), and confirmation that no `.lua` PARSE_ERROR appears.
</output>
