---
phase: 02-conformance-harness-redis-lua-store-defensive-behavior
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - rate-limiter/test/conformance/store-conformance.test.ts
  - rate-limiter/test/redis-integration.test.ts
  - rate-limiter/test/redis-concurrency.test.ts
  - rate-limiter/test/support/redis.ts
  - rate-limiter/vitest.config.ts
autonomous: true
requirements: [TEST-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "One parametrized conformance suite drives identical (key,cost,now) sequences against MemoryStore AND RedisStore and asserts identical Decisions"
    - "Integration tests run RedisStore against a real Redis 7.4 via @testcontainers/redis"
    - "A concurrent burst against real Redis admits EXACTLY limit (no read-modify-write over-admission)"
    - "Docker-dependent suites skip cleanly when Docker is unavailable so the rest of npm test still gates"
  artifacts:
    - path: "rate-limiter/test/conformance/store-conformance.test.ts"
      provides: "describe.each over [MemoryStore, RedisStore] asserting identical Decisions"
      contains: "describe.each"
    - path: "rate-limiter/test/redis-concurrency.test.ts"
      provides: "real-Redis burst admits exactly limit"
      contains: "burst"
    - path: "rate-limiter/test/support/redis.ts"
      provides: "container lifecycle + dockerAvailable helper"
      contains: "RedisContainer"
  key_links:
    - from: "rate-limiter/test/conformance/store-conformance.test.ts"
      to: "sequences.ts fixtures + RedisStore + MemoryStore"
      via: "describe.each([MemoryStore, RedisStore]) driving FakeClock.setTime"
      pattern: "RedisStore"
    - from: "rate-limiter/test/redis-concurrency.test.ts"
      to: "single-Lua-script atomicity"
      via: "Promise.all burst on a shared client"
      pattern: "Promise.all"
---

<objective>
Author the parametrized conformance suite (TEST-02 — the parity contract) plus the
real-Redis integration (TEST-03) and concurrency (TEST-04) tests. The conformance suite
runs identical `(key, cost, now)` sequences from `sequences.ts` against BOTH stores and
asserts identical `Decision`s, catching any TS↔Lua drift. The concurrency test proves the
distributed half of the Core Value: a burst admits exactly `limit` via single-Lua-script
atomicity.

Purpose: Prove bit-for-bit parity between MemoryStore and RedisStore and exact-limit
admission against a real Redis (TEST-02/03/04).
Output: The conformance + integration + concurrency test files, a shared container helper,
and a vitest config update for the longer integration timeouts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-conformance-harness-redis-lua-store-defensive-behavior/02-RESEARCH.md
@.planning/phases/02-conformance-harness-redis-lua-store-defensive-behavior/02-PATTERNS.md

<interfaces>
<!-- Fixtures from plan 02-02 (test/conformance/sequences.ts): -->
type Step = { now: number; cost: number; key: string };
type AlgoCase = { name: string; make: (store: Store, clock: FakeClock) => RateLimiter; steps: Step[] };
export const tbCases, swCases, fwCases: AlgoCase[];

<!-- RedisStore from plan 02-03 (src/index.ts): new RedisStore(config, clock) implements async Store; has close()/disconnect() -->
<!-- MemoryStore + FakeClock + the three limiters: from src/index.js -->

<!-- testcontainers lifecycle (02-RESEARCH.md §"Testcontainers Integration" L336-345): -->
new RedisContainer("redis:7.4-alpine").start(); container.getConnectionUrl(); container.stop();

<!-- burst helper shape (test/concurrency.test.ts L36-42): fire N≫limit overlapping consume(sameKey), count allowed -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Shared container helper + parametrized conformance suite (TEST-02)</name>
  <read_first>
    - rate-limiter/test/conformance/sequences.ts (the fixtures from plan 02-02 — the input + the shared expected-value source)
    - rate-limiter/test/token-bucket.test.ts (the FakeClock.setTime drive pattern + import convention L9-21)
    - .planning/phases/02-...-defensive-behavior/02-RESEARCH.md §"Conformance Harness Shape" (L285-331) + §"Testcontainers Integration" (L333-347) + §"Pitfall 5" (one container per file)
    - .planning/phases/02-...-defensive-behavior/02-PATTERNS.md §"test/conformance/*" + §"Test setup + import convention"
    - rate-limiter/vitest.config.ts (current config — to extend testTimeout for integration files)
  </read_first>
  <action>
    Create `test/support/redis.ts` exporting: a `dockerAvailable()` preflight (detect a
    reachable Docker daemon; used by `describe.skipIf`), and helpers to start
    `new RedisContainer("redis:7.4-alpine").start()` / build a `RedisStore` pointed at
    `container.getConnectionUrl()` with an injected `FakeClock` / stop the container and
    `close()` the store. Create `test/conformance/store-conformance.test.ts`: a
    `describe.each` over `[["MemoryStore", makeMemory], ["RedisStore", makeRedis]]` (skip the
    RedisStore param via `describe.skipIf(!dockerAvailable())`), nested `describe.each` over
    `[...tbCases, ...swCases, ...fwCases]`. For each case: build the limiter via `c.make(store,
    clock)`, iterate `steps`, `clock.setTime(step.now)`, `await limiter.consume(step.key,
    step.cost)`, and assert the FULL `Decision` with `toEqual` against a SHARED expected value
    computed once (so any TS↔Lua drift — a wrong floor/ceil, a lost fraction — fails). Start
    ONE Redis container in `beforeAll`/stop in `afterAll`; isolate cases with `flushall` or
    unique keys (Pitfall 5). Drive `now` via `FakeClock.setTime` (NOT vi fake timers). Extend
    `vitest.config.ts` so the integration/conformance files get a generous `testTimeout`
    (e.g. 30-60s) for container startup.
  </action>
  <verify>
    <automated>cd rate-limiter && npm run typecheck && npx vitest run test/conformance/store-conformance.test.ts && grep -q "describe.each" test/conformance/store-conformance.test.ts && grep -q "RedisContainer" test/support/redis.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` exits 0
    - `vitest run test/conformance/store-conformance.test.ts` exits 0 — the RedisStore param produces `Decision`s IDENTICAL to MemoryStore for every fixture (incl. the Xu sliding-window anchor: admit, remaining=1), OR the RedisStore param is reported skipped if Docker is absent (MemoryStore param still runs and passes)
    - the suite asserts the whole `Decision` with `toEqual` against a single shared expected value (not per-store expectations)
    - `test/support/redis.ts` exports `dockerAvailable` and uses `RedisContainer("redis:7.4-alpine")`
  </acceptance_criteria>
  <done>One parametrized suite drives both stores through the shared fixtures and asserts identical Decisions (TS↔Lua parity), skipping the Redis param cleanly without Docker.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Real-Redis integration happy-path (TEST-03) + concurrency over-admission guard (TEST-04)</name>
  <read_first>
    - rate-limiter/test/support/redis.ts (the container helper from Task 1)
    - rate-limiter/test/concurrency.test.ts (the `burst()` helper L36-42 + the atomicity header L1-24 — mirror the framing; here atomicity = single-Lua-script, not event loop)
    - rate-limiter/test/token-bucket.test.ts (assertion style — exact integers)
    - .planning/phases/02-...-defensive-behavior/02-RESEARCH.md §"Concurrency over real Redis" (L365-366) + §"Testcontainers Integration"
    - .planning/phases/02-...-defensive-behavior/02-PATTERNS.md §"test/redis-integration.test.ts" + §"test/redis-concurrency.test.ts"
  </read_first>
  <action>
    Create `test/redis-integration.test.ts` (guarded by `describe.skipIf(!dockerAvailable())`,
    one container in `beforeAll`/`afterAll`): exercise each algorithm's happy path against the
    real `RedisStore` — admit up to limit, reject past it, confirm TTL/state survives across
    calls within the window, and assert integer `resetMs`/`retryAfterMs`. Create
    `test/redis-concurrency.test.ts` (same guard + container): reuse the `burst()` shape — fire
    N≫limit overlapping `consume(sameKey)` with a FIXED `now` against a `RedisStore`, assert
    EXACTLY `limit` admitted for token-bucket and fixed-window (the distributed over-admission
    guard — atomicity comes from single-Lua-script execution serialized by Redis, per the
    concurrency.test.ts header). Mirror that header's framing in a comment block. Import the
    burst helper or re-declare it locally.
  </action>
  <verify>
    <automated>cd rate-limiter && npm run typecheck && npx vitest run test/redis-integration.test.ts test/redis-concurrency.test.ts && grep -q "burst" test/redis-concurrency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` exits 0
    - With Docker present: `vitest run test/redis-integration.test.ts test/redis-concurrency.test.ts` exits 0 — the burst admits EXACTLY `limit` (never `limit + k`) against real Redis, and the integration suite confirms admit/reject + integer durations
    - Without Docker: both files report skipped (no failure), so `npm test` still gates the rest
    - `test/redis-concurrency.test.ts` uses a `Promise.all` burst on the shared client
  </acceptance_criteria>
  <done>Real-Redis integration proves per-algorithm admit/reject + state survival, and the concurrency burst proves exactly-`limit` admission via single-Lua-script atomicity (TEST-03/04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → testcontainers → Docker daemon | tests control a real container |
| concurrent clients → shared ioredis client → Redis | the race the atomicity guard rules out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering/Race | concurrent RedisStore ops | mitigate | single-Lua-script execution serializes the read-modify-write in Redis; the concurrency test asserts exactly-`limit` admission (catches over-admission, the highest-severity limiter bug) |
| T-02-13 | Repudiation/Parity | TS↔Lua drift | mitigate | conformance suite asserts the FULL Decision `toEqual` a shared expected value across both stores — any divergent floor/ceil or lost fraction fails immediately |
| T-02-14 | Availability (test infra) | missing Docker daemon | accept | `describe.skipIf(!dockerAvailable())` lets the non-Docker suite still gate; the phase success criteria require these pass where Docker is present (documented preflight) |
</threat_model>

<verification>
- `npm run typecheck` passes.
- With Docker: conformance + integration + concurrency suites pass (RedisStore matches
  MemoryStore bit-for-bit; real-Redis burst admits exactly `limit`).
- Without Docker: Redis-dependent suites skip cleanly; `npm test` still green on the rest.
</verification>

<success_criteria>
- TEST-02: one parametrized suite proves identical Decisions across both stores.
- TEST-03: RedisStore exercised against a real `redis:7.4-alpine` container.
- TEST-04: a real-Redis burst admits exactly `limit`.
- Docker-absent runs skip gracefully. `tsc --noEmit` green.
</success_criteria>

<output>
Create `.planning/phases/02-conformance-harness-redis-lua-store-defensive-behavior/02-04-SUMMARY.md` when done
</output>
