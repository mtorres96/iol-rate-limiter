---
phase: 02-conformance-harness-redis-lua-store-defensive-behavior
plan: 05
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - rate-limiter/test/fault-injection.test.ts
  - rate-limiter/test/support/docker-pause.ts
autonomous: true
requirements: [DEF-01, DEF-02, TEST-05]
must_haves:
  truths:
    - "Under a DOWN Redis (container.stop), fail-open admits and fail-closed denies, with no unhandled rejection"
    - "Under a SLOW Redis (dockerode pause → commandTimeout breach), fail-open admits and fail-closed denies, with no unhandled rejection"
    - "Repeated failures open the circuit breaker (subsequent calls short-circuit without hitting Redis); recovery half-opens and closes"
    - "RedisStore never rejects in any fault cell (every consume resolves)"
  artifacts:
    - path: "rate-limiter/test/fault-injection.test.ts"
      provides: "down/slow × fail-open/fail-closed × breaker matrix (TEST-05)"
      contains: "fail-closed"
    - path: "rate-limiter/test/support/docker-pause.ts"
      provides: "pause/unpause a started container (dockerode fallback) for the slow simulation"
      contains: "pause"
  key_links:
    - from: "rate-limiter/test/fault-injection.test.ts"
      to: "RedisStore degraded policy + breaker"
      via: "container.stop() / dockerode pause() then assert resolves"
      pattern: "resolves"
---

<objective>
Author the fault-injection matrix (TEST-05): drive a real Redis DOWN (container stop) and
SLOW (dockerode pause → `commandTimeout` breach) and assert the `RedisStore` degrades
correctly under BOTH the fail-open and fail-closed policies AND that the circuit breaker
opens during the outage and recovers afterward — with no unhandled rejection in any cell
(DEF-01/DEF-02).

Purpose: Prove the defensive layer behaves correctly under induced Redis failure — the
fourth phase success criterion.
Output: `fault-injection.test.ts` and a small `docker-pause` test helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-conformance-harness-redis-lua-store-defensive-behavior/02-RESEARCH.md
@.planning/phases/02-conformance-harness-redis-lua-store-defensive-behavior/02-PATTERNS.md

<interfaces>
<!-- Container helper from plan 02-04 (test/support/redis.ts): dockerAvailable(), start container, build RedisStore(config, clock), stop/close -->
<!-- RedisStore from plan 02-03: degraded() policy path + injected Clock breaker; close()/disconnect() for teardown -->

<!-- Fault mechanics (02-RESEARCH.md §"Testcontainers Integration & Fault Injection" L333-377): -->
<!--   DOWN: await container.stop()  (connection refused; enableOfflineQueue:false + maxRetriesPerRequest:1 → errors fast) -->
<!--   SLOW: dockerode getContainer(container.getId()).pause()/unpause()  (cgroups freeze keeps TCP open → commandTimeout fires) -->
<!--   A3: testcontainers 12 may expose pause() directly — try (container as any).pause first, else dockerode fallback -->

<!-- Fault matrix (02-RESEARCH.md L371-377): -->
<!--   Down  × fail-open → allowed:true ; × fail-closed → allowed:false ; 5 failures → breaker OPEN, short-circuits within cooldown -->
<!--   Slow  × fail-open → allowed:true ; × fail-closed → allowed:false ; timeouts open breaker; unpause → half-open probe → CLOSED -->
<!-- Assert expect(...).resolves everywhere (no unhandled rejection — DEF-02). Inject FakeClock into the breaker for deterministic half-open. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: docker-pause helper (pause/unpause a started container)</name>
  <read_first>
    - rate-limiter/test/support/redis.ts (the container helper + getId access from plan 02-04)
    - .planning/phases/02-...-defensive-behavior/02-RESEARCH.md §'"Redis slow" (commandTimeout breach)' (L351-363) + §"Open Questions" Q1 (try container.pause first, else dockerode) + §"Assumptions Log" A3
    - .planning/phases/02-...-defensive-behavior/02-PATTERNS.md §"test/fault-injection.test.ts" (No Analog Found — wholly new mechanism)
  </read_first>
  <action>
    Create `test/support/docker-pause.ts` exporting `pause(container)` / `unpause(container)`
    that freeze/unfreeze a started testcontainers container so an in-flight ioredis command
    hangs and `commandTimeout` fires (the SLOW simulation). Per 02-RESEARCH A3/Q1: first try a
    first-class `(container as any).pause?.()/unpause?.()` if the installed testcontainers 12
    exposes it; otherwise fall back to dockerode — `new Docker().getContainer(container.getId()).
    pause()/unpause()` (dockerode is a transitive dep already installed). Keep it a thin, typed
    wrapper. Verify the chosen path against the installed `testcontainers` 12.0.3 typings during
    implementation.
  </action>
  <verify>
    <automated>cd rate-limiter && npm run typecheck && test -s test/support/docker-pause.ts && grep -q "pause" test/support/docker-pause.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` exits 0
    - `test/support/docker-pause.ts` exports `pause`/`unpause` and resolves either to a native testcontainers pause or the dockerode `getContainer(id).pause()` fallback
  </acceptance_criteria>
  <done>A typed pause/unpause helper exists that freezes a running container (native-or-dockerode) to drive the commandTimeout breach.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fault-injection matrix — down/slow × fail-open/fail-closed × breaker (TEST-05)</name>
  <read_first>
    - rate-limiter/test/support/redis.ts (container + RedisStore builder) and test/support/docker-pause.ts (Task 1)
    - rate-limiter/src/store/redis.ts (the degraded() policy + breaker gate being exercised)
    - rate-limiter/src/store/breaker.ts (defaults 5/2000; injected Clock for deterministic half-open)
    - .planning/phases/02-...-defensive-behavior/02-RESEARCH.md §"Fault-injection matrix" (L368-377) + §"Pitfall 4" (timeout headroom)
    - .planning/phases/02-...-defensive-behavior/02-PATTERNS.md §"test/fault-injection.test.ts"
  </read_first>
  <action>
    Create `test/fault-injection.test.ts` (guarded by `describe.skipIf(!dockerAvailable())`,
    one container in `beforeAll`/`afterAll`, an injected `FakeClock` into each RedisStore's
    breaker for deterministic cooldown). Cover every matrix cell from 02-RESEARCH §"Fault
    matrix": (a) DOWN via `container.stop()` — fail-open RedisStore `consume` resolves
    `allowed:true`; fail-closed resolves `allowed:false`; (b) SLOW via `pause()`/`unpause()`
    from Task 1 — fail-open resolves `allowed:true` (timeout → admit), fail-closed resolves
    `allowed:false`; (c) BREAKER — after `failureThreshold` (5) consecutive failures the
    breaker OPENs and subsequent calls short-circuit WITHOUT a Redis attempt within the
    cooldown, then advancing the injected FakeClock past `cooldownMs` + an `unpause()`/`restart()`
    lets a half-open probe succeed → CLOSED. Assert `await expect(store.<op>(...)).resolves...`
    in EVERY cell (RedisStore must never reject — DEF-02). Use the 75ms commandTimeout headroom
    (Pitfall 4). Restart/unpause the container between cells so state is clean.
  </action>
  <verify>
    <automated>cd rate-limiter && npm run typecheck && npx vitest run test/fault-injection.test.ts && grep -q "fail-closed" test/fault-injection.test.ts && grep -q "resolves" test/fault-injection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` exits 0
    - With Docker: `vitest run test/fault-injection.test.ts` exits 0 — every cell (down/slow × fail-open/fail-closed) resolves with the correct `allowed` value and NO unhandled rejection; the breaker opens after 5 failures (short-circuits) and recovers via half-open on the injected FakeClock
    - Without Docker: the file reports skipped (no failure)
    - every fault assertion uses `expect(...).resolves` (proving RedisStore never rejects)
  </acceptance_criteria>
  <done>The full down/slow × fail-open/fail-closed × breaker matrix passes against real Redis with no unhandled rejection, proving the defensive layer (DEF-01/DEF-02, TEST-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| induced Redis outage → RedisStore policy | a down/slow store decides admit vs deny |
| test → dockerode/testcontainers → Docker | tests freeze/stop a real container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Elevation/Bypass | fail-open under outage | accept | fail-open admitting under a down/slow Redis is the INTENDED configurable default (D2-04); this test PROVES the trade-off is bounded (fail-closed is available and verified) — DESIGN.md (Phase 4) documents the rationale |
| T-02-16 | Denial of Service | unhandled rejection on store failure | mitigate | every fault cell asserts `expect(...).resolves` — RedisStore catches all errors/timeouts and resolves through the policy (DEF-02), so a Redis outage can never crash the caller |
| T-02-17 | Denial of Service | timeout pile-up during outage | mitigate | the breaker opens after 5 failures and short-circuits (no further Redis attempts within cooldown) — proven in the breaker cell (D2-05) |
</threat_model>

<verification>
- `npm run typecheck` passes.
- With Docker: the fault matrix passes — down/slow under both policies degrade correctly,
  the breaker opens and recovers, and no cell produces an unhandled rejection.
- Without Docker: the suite skips cleanly.
- Full `npm test` green (all of Phase 2's suites together).
</verification>

<success_criteria>
- TEST-05: down + slow faults proven under BOTH fail-open and fail-closed, plus the breaker
  open/recover cycle.
- DEF-01/DEF-02: every Redis call is timeout-bounded and every fault cell resolves (no
  unhandled rejection).
- `tsc --noEmit` + full Vitest suite green (`npm run verify` posture).
</success_criteria>

<output>
Create `.planning/phases/02-conformance-harness-redis-lua-store-defensive-behavior/02-05-SUMMARY.md` when done
</output>
