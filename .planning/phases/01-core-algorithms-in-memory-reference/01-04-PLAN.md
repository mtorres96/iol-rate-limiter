---
phase: 01-core-algorithms-in-memory-reference
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - rate-limiter/test/token-bucket.test.ts
  - rate-limiter/test/sliding-window.test.ts
  - rate-limiter/test/fixed-window.test.ts
  - rate-limiter/test/concurrency.test.ts
autonomous: true
requirements: [TEST-01, ALGO-01, ALGO-02, ALGO-03]
must_haves:
  truths:
    - "FakeClock-driven tests (no real sleeps) demonstrate refill, burst, request cost, exact-limit boundary, and cost>capacity graceful reject for Token Bucket"
    - "A test reproduces Xu Ch.4's Sliding Window worked example verbatim (limit=7, prev=5, curr=3, 50% in → admit, remaining=1) plus window rollover"
    - "A test explicitly demonstrates Fixed Window's 2x boundary-burst as documented required behavior"
    - "A concurrency test fires N>limit overlapping consume calls and asserts exactly limit resolve allowed:true, with the event-loop-atomicity justification commented"
    - "The full suite passes (`vitest run` exits 0) and asserts exact integer resetMs/retryAfterMs/remaining values"
  artifacts:
    - path: "rate-limiter/test/token-bucket.test.ts"
      provides: "Token Bucket determinism: refill, burst, cost, exact-limit, cost>capacity, rounding"
    - path: "rate-limiter/test/sliding-window.test.ts"
      provides: "Sliding Window: Xu Ch.4 pinned example + rollover + estimate-floor boundary"
    - path: "rate-limiter/test/fixed-window.test.ts"
      provides: "Fixed Window: rollover + explicit 2x boundary-burst demonstration"
    - path: "rate-limiter/test/concurrency.test.ts"
      provides: "Event-loop atomicity / exact-limit over-admission guard (memory half)"
  key_links:
    - from: "rate-limiter/test/sliding-window.test.ts"
      to: "SlidingWindowLimiter"
      via: "asserts the D-14 pinned numbers"
      pattern: "limit:\\s*7|prev|5\\.5|remaining"
    - from: "rate-limiter/test/concurrency.test.ts"
      to: "RateLimiter.consume"
      via: "Promise.all burst counts exactly limit allowed"
      pattern: "Promise.all"
---

<objective>
Prove the three algorithms correct with exhaustive deterministic FakeClock tests (no real sleeps) and prove the in-memory exact-limit guard with a concurrency burst test — the stated Core Value of the deliverable.

Purpose: TEST-01 (comprehensive Vitest unit tests for all three algorithms: refill, burst, rollover, cost, exact-limit, FakeClock, no sleeps) and Success Criteria 3 (FakeClock demonstrations including Fixed Window's boundary-burst) and 4 (memory half of the over-admission guard). The Xu Ch.4 pinned example (D-14) is the regression anchor that makes Phase-2 TS↔Lua conformance unambiguous.
Output: four test files under `rate-limiter/test/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-algorithms-in-memory-reference/01-CONTEXT.md
@.planning/phases/01-core-algorithms-in-memory-reference/01-RESEARCH.md
@.planning/phases/01-core-algorithms-in-memory-reference/01-PATTERNS.md
@CLAUDE.md
@rate-limiter/src/index.ts
@rate-limiter/src/store/memory.ts
@rate-limiter/src/clock.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Token Bucket + Sliding Window + Fixed Window deterministic tests</name>
  <files>rate-limiter/test/token-bucket.test.ts, rate-limiter/test/sliding-window.test.ts, rate-limiter/test/fixed-window.test.ts</files>
  <read_first>
    - rate-limiter/src/index.ts (the public surface to import: limiters, MemoryStore, FakeClock)
    - rate-limiter/src/store/memory.ts (the exact op math + the pinned rounding contract the tests assert against)
    - .planning/phases/01-core-algorithms-in-memory-reference/01-PATTERNS.md section "rate-limiter/test/*.test.ts" (the per-file assertion table + the FakeClock test usage shape)
    - .planning/phases/01-core-algorithms-in-memory-reference/01-CONTEXT.md decisions D-01, D-02, D-04, D-05, D-10, D-11, D-13, D-14, and "Claude's Discretion → Fixed Window boundary-burst is REQUIRED"
    - .planning/phases/01-core-algorithms-in-memory-reference/01-RESEARCH.md "Pattern 2", "Pitfall 3 (rounding)", "Pitfall 4 (Fixed Window boundary-burst)", and "Code Examples → Sliding Window worked example as a test"
    - CLAUDE.md "Testing time + concurrency (prescriptive)" (FakeClock, no real sleeps)
  </read_first>
  <action>
    All tests import from the package source (the barrel `../src/index.js` or specific modules), construct a fresh `MemoryStore` and a `FakeClock(0)` per test, inject the FakeClock into the limiter, and advance time ONLY via `clock.tick(ms)` / `clock.setTime(ms)` — NEVER `vi.useFakeTimers`, `setTimeout`, or any real sleep (Pitfall 1).

    `token-bucket.test.ts` (ALGO-01; D-01/D-02/D-04/D-05/D-10): assert (a) lazy refill — consume to empty, `clock.tick` enough to refill N tokens, next consume sees exactly N available; (b) burst to capacity at t=0; (c) request `cost` > 1 consumes the right number and floors `remaining`; (d) exact-limit boundary — the call that exactly empties the bucket is `allowed:true`, the next is `allowed:false`; (e) `cost > capacity` returns `allowed:false` WITHOUT consuming (state unchanged) and reports a best-effort `retryAfterMs` (D-02); (f) assert the EXACT integer `resetMs` and `retryAfterMs` values implied by the `Math.ceil` rounding contract, and that `remaining` is an integer (`Math.floor`) — so Phase-2 Lua has a precise target.

    `sliding-window.test.ts` (ALGO-02; D-13/D-14): the pinned regression anchor — a test named for Xu Ch.4 that sets up `limit=7`, previous window = 5 requests, current window = 3 requests, 50% (30s of a 60s window) into the current window, then asserts a new `cost:1` request is admitted because `floor(3 + 5*0.5) = floor(5.5) = 5` and `5 + 1 = 6 <= 7`, with `remaining === 1`. Add (a) window rollover — curr counts shift to prev when the window advances; (b) estimate-floor boundary — a case at the `floor(estimate)+cost == limit` edge admits and one just past rejects; (c) on reject, state (curr count) is unchanged (D-01).

    `fixed-window.test.ts` (ALGO-03; Pitfall 4): assert (a) within a window, admit while `count + cost <= limit`, reject at the boundary; (b) window rollover resets the count when the bucket index changes; (c) the EXPLICIT 2x boundary-burst demonstration — admit `limit` requests near the end of window N, `clock.tick` across the window boundary, admit `limit` again at the start of window N+1, and assert that roughly `2*limit` were admitted within a short real-time span — comment that this is the KNOWN, REQUIRED Fixed Window tradeoff (the reason Sliding Window exists), NOT a bug to fix.
  </action>
  <verify>
    <automated>cd rate-limiter && npx vitest run test/token-bucket.test.ts test/sliding-window.test.ts test/fixed-window.test.ts && node -e "const fs=require('fs'); const sw=fs.readFileSync('test/sliding-window.test.ts','utf8'); if(!/Xu|Ch\.?\s?4|prev/i.test(sw)||!/\b7\b/.test(sw)){console.error('SW pinned example missing');process.exit(1)} const all=['token-bucket','sliding-window','fixed-window'].map(f=>fs.readFileSync('test/'+f+'.test.ts','utf8')).join('\n'); if(/useFakeTimers|setTimeout|new Promise\(.*setTimeout/.test(all)){console.error('real timer/sleep in tests');process.exit(2)} const fw=fs.readFileSync('test/fixed-window.test.ts','utf8'); if(!/boundary|burst/i.test(fw)){console.error('FW boundary-burst test missing');process.exit(3)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd rate-limiter && npx vitest run` for the three files exits 0.
    - `sliding-window.test.ts` reproduces the Xu Ch.4 numbers (limit 7, prev 5, curr 3, 50% in → admit, remaining 1).
    - `fixed-window.test.ts` contains an explicit boundary-burst test admitting ~2×limit across the boundary, commented as required behavior.
    - `token-bucket.test.ts` asserts exact integer `resetMs`/`retryAfterMs` and a floored integer `remaining`, plus the `cost>capacity` graceful-reject case.
    - No `vi.useFakeTimers`, `setTimeout`, or real sleep anywhere in the three files.
  </acceptance_criteria>
  <done>All three algorithms have deterministic FakeClock test coverage for refill/burst/rollover/cost/exact-limit/cost>capacity, with the Xu pinned example and the Fixed Window boundary-burst demonstrated, and exact integer rounding assertions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Concurrency / exact-limit over-admission guard test</name>
  <files>rate-limiter/test/concurrency.test.ts</files>
  <read_first>
    - rate-limiter/src/index.ts (limiters + MemoryStore + FakeClock to import)
    - rate-limiter/src/store/memory.ts (to confirm the op is a single synchronous critical section with no await — the property under test)
    - .planning/phases/01-core-algorithms-in-memory-reference/01-PATTERNS.md "concurrency.test.ts" row + the "Event-loop atomicity, not locks" shared pattern
    - .planning/phases/01-core-algorithms-in-memory-reference/01-RESEARCH.md "Pitfall 2: Mis-modeling the concurrency test as OS-thread parallelism" (the honest single-thread proof + the optional torn read-modify-write reinforcement)
    - CLAUDE.md "Testing time + concurrency (prescriptive)" (fire N overlapping Promise.all, assert exactly `limit` allowed)
  </read_first>
  <action>
    Create `rate-limiter/test/concurrency.test.ts`. With a fixed `FakeClock` (no time advance during the burst, so refill cannot mask the guard) and a small `limit`/`capacity`, fire `N > limit` overlapping `consume(key)` calls on the SAME key via `Promise.all`, then assert EXACTLY `limit` of the resolved decisions have `allowed:true` and the remainder `allowed:false` — never `limit + k`. Run this for at least the Token Bucket limiter and one window limiter to show the guard is algorithm-general. Add a clear comment block stating the justification per Pitfall 2: because each op is one synchronous read-modify-write critical section with NO `await` inside, no two interleaved calls observe stale state, so over-admission is impossible — this proves EVENT-LOOP atomicity for the in-memory store (distinct from the Lua atomicity Phase 2 proves against a real multi-client Redis race). Optionally add the reinforcement test described in Pitfall 2 (call the op twice without an intervening write to document why a torn read-modify-write would over-admit, and that the real op never does that). Do NOT introduce a mutex/lock or any real timer.
  </action>
  <verify>
    <automated>cd rate-limiter && npx vitest run test/concurrency.test.ts && node -e "const s=require('fs').readFileSync('test/concurrency.test.ts','utf8'); ['Promise.all','allowed'].forEach(t=>{if(!s.includes(t)){console.error('missing '+t);process.exit(1)}}); if(!/event.?loop|atomic/i.test(s)){console.error('event-loop-atomicity justification comment missing');process.exit(2)} if(/useFakeTimers|setTimeout/.test(s)){console.error('real timer in concurrency test');process.exit(3)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd rate-limiter && npx vitest run test/concurrency.test.ts` exits 0.
    - The test fires `N > limit` overlapping `consume` calls via `Promise.all` and asserts exactly `limit` resolve `allowed:true`.
    - A comment justifies the guarantee as event-loop atomicity (single synchronous critical section, no await inside the op), distinct from Phase-2 Redis/Lua atomicity.
    - The guard is exercised for at least two algorithms; no mutex/lock and no real timer used.
  </acceptance_criteria>
  <done>The over-admission guard is proven: a concurrent burst admits exactly `limit`, with the event-loop-atomicity reasoning documented in the test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → core API | tests exercise the public `consume` surface; no external/network surface in this headless library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Elevation (core security property) | over-admission under concurrent burst | mitigate | The concurrency test is the executable proof of the exact-limit guard (Success Criterion 4 memory half) — a logic bug in the guard is the highest-severity rate-limiter threat and this test would fail on it |
| T-01-11 | Repudiation / drift | TS↔Lua rounding drift undermining Phase-2 conformance | mitigate | Tests assert EXACT integer `resetMs`/`retryAfterMs`/`remaining` values and pin the Xu Ch.4 example (D-14), giving the Phase-2 Lua port a precise, unambiguous regression target |
| T-01-12 | Tampering | flaky/real-time tests masking a correctness regression | mitigate | All time is driven by injected `FakeClock` (no `vi.useFakeTimers`, no `setTimeout`, no sleep); verify gates fail if a real timer appears |
</threat_model>

<verification>
- `cd rate-limiter && npx vitest run` exits 0 (the full suite passes).
- `cd rate-limiter && npx tsc --noEmit` exits 0 (the type gate still passes with tests included).
- The Sliding Window Xu example and the Fixed Window boundary-burst are present and asserted.
- The concurrency burst admits exactly `limit`; no real timers anywhere in `test/`.
</verification>

<success_criteria>
The Core Value is proven: deterministic FakeClock tests (no real sleeps) demonstrate refill, burst, window rollover, request cost, exact-limit boundary, the Xu Ch.4 sliding-window example, and Fixed Window's documented boundary-burst (TEST-01, Success Criterion 3); a concurrent burst admits exactly `limit` (Success Criterion 4 memory half); the full Vitest suite and the `tsc --noEmit` gate pass on a clean checkout.
</success_criteria>

<output>
Create `.planning/phases/01-core-algorithms-in-memory-reference/01-04-SUMMARY.md` when done.
</output>
