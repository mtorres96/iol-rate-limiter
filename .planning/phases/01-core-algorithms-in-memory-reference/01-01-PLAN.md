---
phase: 01-core-algorithms-in-memory-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rate-limiter/package.json
  - rate-limiter/tsconfig.json
  - rate-limiter/tsup.config.ts
  - rate-limiter/vitest.config.ts
  - rate-limiter/eslint.config.js
  - rate-limiter/.prettierrc
  - rate-limiter/.nvmrc
  - rate-limiter/.gitignore
autonomous: true
requirements: [DELIV-05]
must_haves:
  truths:
    - "The /rate-limiter package exists and `tsc --noEmit` runs (exits 0 on an empty/valid src)"
    - "`npm install` resolves all locked dev dependencies with zero runtime dependencies"
    - "`npx vitest run` executes (zero tests is a passing run at this stage)"
  artifacts:
    - path: "rate-limiter/package.json"
      provides: "ESM package manifest with locked dev deps, exports map, typecheck/test/build/lint scripts"
      contains: "\"type\": \"module\""
    - path: "rate-limiter/tsconfig.json"
      provides: "strict ESM tsconfig that is the tsc --noEmit gate"
      contains: "\"noEmit\": true"
    - path: "rate-limiter/tsup.config.ts"
      provides: "ESM-only build with dts emit"
    - path: "rate-limiter/vitest.config.ts"
      provides: "node-env Vitest config with v8 coverage over src"
    - path: "rate-limiter/eslint.config.js"
      provides: "ESLint 10 flat config via tseslint helper + prettier last"
  key_links:
    - from: "rate-limiter/package.json"
      to: "rate-limiter/tsconfig.json"
      via: "typecheck script runs `tsc --noEmit`"
      pattern: "tsc --noEmit"
    - from: "rate-limiter/tsup.config.ts"
      to: "rate-limiter/src/index.ts"
      via: "entry points at the public barrel"
      pattern: "src/index.ts"
---

<objective>
Bootstrap the `/rate-limiter` ESM package scaffold so every downstream plan has a working `tsc --noEmit` type gate, a Vitest runner, and a tsup build — with zero runtime dependencies and the exact stack pins locked in CLAUDE.md.

Purpose: Success Criterion 1 ("all code under `/rate-limiter`; `tsc --noEmit` passes on a clean checkout") and DELIV-05 require this scaffold to exist before any algorithm code. It is the build-green gate infrastructure for the whole phase.
Output: A populated `/rate-limiter` package root (manifest + all config files) with installed dev dependencies, ready for `src/` code in later plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-algorithms-in-memory-reference/01-RESEARCH.md
@.planning/phases/01-core-algorithms-in-memory-reference/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the package manifest and dev-dependency set</name>
  <files>rate-limiter/package.json, rate-limiter/.nvmrc, rate-limiter/.gitignore</files>
  <read_first>
    - CLAUDE.md sections "Technology Stack", "Recommended Stack", "Installation", "What NOT to Use" (the exact version pins and the dependency blocklist are the source of truth)
    - .planning/phases/01-core-algorithms-in-memory-reference/01-PATTERNS.md section "Build/Config File Patterns → package.json (essentials)" and the dev-deps note beneath it
    - .planning/phases/01-core-algorithms-in-memory-reference/01-RESEARCH.md section "Standard Stack → Installation" (the dev-only install command) and "Package Legitimacy Audit"
  </read_first>
  <action>
    Create `rate-limiter/package.json` as an ESM package: set `"name": "rate-limiter"`, `"type": "module"`, `"private": true`, an `exports` map with `"."` pointing `types` → `./dist/index.d.ts` and `import` → `./dist/index.js`, top-level `"types": "./dist/index.d.ts"`, and `scripts`: `typecheck` = `tsc --noEmit`, `test` = `vitest run`, `test:watch` = `vitest`, `coverage` = `vitest run --coverage`, `build` = `tsup`, `lint` = `eslint .`, `format` = `prettier --write .`. The package MUST declare ZERO runtime `dependencies`. Install ONLY devDependencies at the CLAUDE.md-locked pins via the RESEARCH "Installation" command: `typescript@~5.9`, `vitest@^4.1`, `@vitest/coverage-v8` pinned to the EXACT resolved vitest version (its peer range is the exact version, not a caret), `tsup@^8.5`, `eslint@^10.5`, `typescript-eslint@^8.62`, `prettier@^3.8`, `eslint-config-prettier@^10.1`, `@types/node@^24`. Do NOT add `tsx` (no demo server this phase). Do NOT add `ioredis`, `express`, `ioredis-mock`, `express-rate-limit`, or `rate-limiter-flexible` (CLAUDE.md "What NOT to Use"). After editing the manifest, run the install from inside `rate-limiter/` so a real `package-lock.json` and `node_modules/` are produced. Create `rate-limiter/.nvmrc` containing `24`. Create `rate-limiter/.gitignore` ignoring `node_modules/`, `dist/`, and `coverage/`.
  </action>
  <verify>
    <automated>cd rate-limiter && npm ls --depth=0 >/dev/null 2>&1; node -e "const p=require('./package.json'); if(p.type!=='module')process.exit(1); if(p.dependencies&&Object.keys(p.dependencies).length)process.exit(2); const d=p.devDependencies||{}; ['typescript','vitest','@vitest/coverage-v8','tsup','eslint','typescript-eslint','prettier','eslint-config-prettier','@types/node'].forEach(k=>{if(!d[k]){console.error('missing devDep '+k);process.exit(3)}}); ['ioredis','express','ioredis-mock','express-rate-limit','rate-limiter-flexible','tsx'].forEach(k=>{if(d[k]){console.error('forbidden dep '+k);process.exit(4)}}); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `rate-limiter/package.json` contains `"type": "module"` and an `exports` map with `"."`.
    - `rate-limiter/package.json` has NO `dependencies` key (or an empty one).
    - All nine locked dev deps are present in `devDependencies`; none of the forbidden packages appear.
    - `rate-limiter/node_modules/` and a lockfile exist after install.
    - `rate-limiter/.nvmrc` equals `24`; `rate-limiter/.gitignore` ignores `node_modules`, `dist`, `coverage`.
  </acceptance_criteria>
  <done>The ESM manifest exists with the exact locked dev-dependency set, zero runtime deps, scripts wired, and dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create the TypeScript, build, test, and lint config files</name>
  <files>rate-limiter/tsconfig.json, rate-limiter/tsup.config.ts, rate-limiter/vitest.config.ts, rate-limiter/eslint.config.js, rate-limiter/.prettierrc</files>
  <read_first>
    - .planning/phases/01-core-algorithms-in-memory-reference/01-PATTERNS.md section "Build/Config File Patterns" (the copy-verbatim tsconfig / tsup / vitest / eslint / .nvmrc shapes)
    - .planning/phases/01-core-algorithms-in-memory-reference/01-RESEARCH.md section "Code Examples" (tsconfig, tsup.config.ts, eslint.config.js, vitest.config.ts) and "Pitfall 5: tsup/tsconfig mismatch breaking the tsc --noEmit gate"
    - CLAUDE.md sections "tsconfig posture (prescriptive)" and the tsup / ESLint flat-config / Prettier rows of the stack tables
  </read_first>
  <action>
    Create `rate-limiter/tsconfig.json` per the PATTERNS "tsconfig.json (strict, ESM, gate-ready)" shape: `target` ES2022, `module` ESNext, `moduleResolution` Bundler, `lib` ["ES2023"], `types` ["node"], `strict` true, `noUncheckedIndexedAccess` true, `verbatimModuleSyntax` true, `declaration` true, `noEmit` true (tsc is the type-gate; tsup does the actual emit), `skipLibCheck` true, `forceConsistentCasingInFileNames` true, `esModuleInterop` true; `include` ["src", "test", "tsup.config.ts", "vitest.config.ts"]. Create `rate-limiter/tsup.config.ts` via `defineConfig` from `tsup`: `entry` ["src/index.ts"], `format` ["esm"] (ESM-only per CONTEXT discretion + CLAUDE.md), `dts` true, `sourcemap` true, `clean` true, `target` "node24"; add a comment that tsdown migration is intentionally declined (CLAUDE.md locks tsup). Create `rate-limiter/vitest.config.ts` via `defineConfig` from `vitest/config`: `test.environment` "node", `test.coverage` provider "v8" with `include` ["src/**"]. Create `rate-limiter/eslint.config.js` (flat config) importing `tseslint` from `typescript-eslint` and `prettier` from `eslint-config-prettier`, exporting `tseslint.config(...tseslint.configs.recommended, prettier)` with `prettier` LAST. Create `rate-limiter/.prettierrc` with a minimal JSON config (e.g. `singleQuote` and `semi` true). Then create a temporary `rate-limiter/src/index.ts` containing a single line `export {};` so `tsc --noEmit` and `vitest run` can execute against a valid empty entry — this placeholder barrel is replaced with real exports in plan 01-02.
  </action>
  <verify>
    <automated>cd rate-limiter && npx tsc --noEmit && npx eslint . && npx vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `rate-limiter/tsconfig.json` contains `"noEmit": true`, `"strict": true`, and `"moduleResolution": "Bundler"`.
    - `cd rate-limiter && npx tsc --noEmit` exits 0.
    - `rate-limiter/tsup.config.ts` has `format: ["esm"]` and `dts: true`.
    - `rate-limiter/eslint.config.js` references `typescript-eslint` and lists `prettier` after the recommended set; `npx eslint .` exits 0.
    - `npx vitest run --passWithNoTests` exits 0.
    - `rate-limiter/src/index.ts` exists as a placeholder (`export {};`).
  </acceptance_criteria>
  <done>All five config files exist matching the locked shapes; the type gate, lint, and test runner all execute cleanly against a placeholder barrel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → npm registry | dev dependencies are pulled from npm at install time (the only external surface this phase has) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm dev-dependency installs | mitigate | All packages pre-audited in RESEARCH "Package Legitimacy Audit" (all Approved, mainstream, known source repos); pins are exact CLAUDE.md versions; lockfile committed for reproducibility |
| T-01-02 | Elevation | forbidden off-the-shelf limiter / ioredis-mock slipping into deps | mitigate | Task 1 verify gate fails (exit 4) if any forbidden package appears in devDependencies |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — RESEARCH audit marks every package Approved with verified source repos; no blocking human checkpoint required |
</threat_model>

<verification>
- `cd rate-limiter && npm run typecheck` exits 0.
- `cd rate-limiter && npm run lint` exits 0.
- `cd rate-limiter && npm test -- --passWithNoTests` exits 0 (no tests yet at this wave).
- `rate-limiter/package.json` declares zero runtime dependencies.
</verification>

<success_criteria>
The `/rate-limiter` ESM package scaffold exists with all locked config files, the exact CLAUDE.md dev-dependency pins installed, zero runtime dependencies, and the `tsc --noEmit` gate + Vitest runner + ESLint flat config all executing cleanly. Downstream plans can now add `src/` code.
</success_criteria>

<output>
Create `.planning/phases/01-core-algorithms-in-memory-reference/01-01-SUMMARY.md` when done.
</output>
