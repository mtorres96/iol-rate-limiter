---
phase: 03-express-middleware-http-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rate-limiter/package.json
  - rate-limiter/tsup.config.ts
autonomous: false
requirements: [HTTP-01]
user_setup: []

must_haves:
  truths:
    - "The four adapter devDeps (express, @types/express, supertest, @types/supertest) are installed and resolvable"
    - "The build emits the Express adapter as a separate dist entry preserving directory structure"
    - "The package advertises a `rate-limiter/express` subpath that resolves to the built adapter"
    - "The core entry (`.`) still resolves and contains no Express"
  artifacts:
    - path: "rate-limiter/package.json"
      provides: "Four new devDeps + `./express` exports subpath + express peerDependency"
      contains: "\"./express\""
    - path: "rate-limiter/tsup.config.ts"
      provides: "Second tsup entry for the Express adapter"
      contains: "src/adapters/express/index.ts"
  key_links:
    - from: "rate-limiter/package.json exports[\"./express\"]"
      to: "dist/adapters/express/index.js"
      via: "subpath export map"
      pattern: "dist/adapters/express/index"
    - from: "rate-limiter/tsup.config.ts entry"
      to: "src/adapters/express/index.ts"
      via: "tsup multi-entry build"
      pattern: "adapters/express/index"
---

<objective>
Install the four Express-adapter devDependencies (behind a single package-legitimacy
checkpoint) and wire the build so the adapter ships as the `rate-limiter/express`
subpath — a second `tsup` entry plus a `package.json` `exports` map entry — without
leaking Express into the core entry.

Purpose: Phase 3's middleware and its supertest suites cannot typecheck, build, or run
until `express`/`@types/express`/`supertest`/`@types/supertest` are installed and the
subpath build wiring exists. This is the foundational wave everything else depends on.
Output: Updated `package.json` (devDeps + `./express` export + `express` peerDependency)
and `tsup.config.ts` (second entry). The `src/adapters/express/index.ts` source itself is
created in plan 03-02; this plan only points the build at it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-express-middleware-http-semantics/03-RESEARCH.md
@.planning/phases/03-express-middleware-http-semantics/03-PATTERNS.md

<interfaces>
<!-- Current package.json exports (rate-limiter/package.json lines 6-11) — extend, do not replace -->
exports: { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } }
<!-- Current tsup entry (rate-limiter/tsup.config.ts line 13) — add a second entry, keep onSuccess Lua copy -->
entry: ['src/index.ts']
<!-- devDependencies are caret-pinned (rate-limiter/package.json lines 22-36); dependencies has ioredis (line 37-39) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for the four adapter devDeps</name>
  <files>(none — verification gate, no files written)</files>
  <read_first>
    - .planning/phases/03-express-middleware-http-semantics/03-RESEARCH.md (§"Package Legitimacy Audit" — the four packages are tagged [ASSUMED] because slopcheck was unavailable at research time)
  </read_first>
  <action>
    STOP and present this package-legitimacy gate to the operator BEFORE the install in
    Task 2 runs. Nothing is installed yet. This is a blocking-human checkpoint (never
    auto-approvable) because slopcheck was unavailable at research time, so all four
    packages are [ASSUMED]. Present the four registry pages below and wait for explicit
    approval.
  </action>
  <what-built>
    Nothing is installed yet. This checkpoint confirms the four package names before the
    install task runs, per the package-legitimacy protocol (slopcheck was unavailable at
    research time, so all four are [ASSUMED]).
  </what-built>
  <how-to-verify>
    Confirm each of these four packages on its registry page before approving install:
    1. https://www.npmjs.com/package/express — expect ~16-year-old package, expressjs/express repo, no postinstall.
    2. https://www.npmjs.com/package/@types/express — DefinitelyTyped, v5.x line.
    3. https://www.npmjs.com/package/supertest — ~14-year-old package, ladjs/supertest repo, no postinstall, ships NO bundled types.
    4. https://www.npmjs.com/package/@types/supertest — DefinitelyTyped, not deprecated (REQUIRED because supertest ships no types).
  </how-to-verify>
  <verify>
    <human-check>Operator has confirmed all four registry pages and typed "approved".</human-check>
  </verify>
  <acceptance_criteria>
    - All four registry pages confirmed legitimate (correct repo, decade-old, no postinstall surprises).
    - Operator types "approved" (or names issues) before the install task in Task 2 runs.
  </acceptance_criteria>
  <done>Operator approved the four [ASSUMED] package names; install in Task 2 may proceed.</done>
  <resume-signal>Type "approved" to proceed with the install, or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the four adapter devDeps + declare express peerDependency</name>
  <files>rate-limiter/package.json</files>
  <read_first>
    - rate-limiter/package.json (current devDependencies caret style lines 22-36; existing dependencies block lines 37-39)
    - .planning/phases/03-express-middleware-http-semantics/03-RESEARCH.md (§"Standard Stack" / §"Installation" — exact versions and the peerDependency recommendation)
  </read_first>
  <action>
    From the `rate-limiter/` directory run `npm install -D express@^5.1 @types/express@^5 supertest@^7.2 @types/supertest@^7.2` (single command, all four devDeps). Then add a `peerDependencies` block to `package.json` declaring `"express": ">=5"` (per RESEARCH recommendation A4 — the adapter requires the consumer to bring Express; keep it in devDeps for tests too). Do NOT add express to runtime `dependencies` (which currently holds only `ioredis`). Verify the four entries land in `devDependencies` caret-pinned, matching the existing style; supertest ships NO bundled types so `@types/supertest` is mandatory, not optional.
  </action>
  <verify>
    <automated>cd rate-limiter && node -e "const p=require('./package.json'); const d=p.devDependencies; ['express','@types/express','supertest','@types/supertest'].forEach(n=>{if(!d[n])throw new Error('missing devDep '+n)}); if(!p.peerDependencies||!p.peerDependencies.express)throw new Error('missing express peerDependency'); if(p.dependencies&&p.dependencies.express)throw new Error('express must NOT be a runtime dependency');" && node -e "require.resolve('express'); require.resolve('supertest');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies contains `express`, `@types/express`, `supertest`, `@types/supertest` (caret-pinned).
    - `package.json` has `peerDependencies.express` set to `">=5"`.
    - `express` is NOT in runtime `dependencies`.
    - `node -e "require.resolve('express')"` and `require.resolve('supertest')` both succeed (deps actually installed into node_modules).
  </acceptance_criteria>
  <done>All four devDeps installed and resolvable; express declared as a peerDependency, not a runtime dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the Express adapter subpath into tsup + package.json exports</name>
  <files>rate-limiter/tsup.config.ts, rate-limiter/package.json</files>
  <read_first>
    - rate-limiter/tsup.config.ts (current single-entry config; the `onSuccess` Lua copy at lines 19-21 must stay UNCHANGED)
    - rate-limiter/package.json (current `exports` map lines 6-11 with only the `.` key)
    - .planning/phases/03-express-middleware-http-semantics/03-RESEARCH.md (§"Pattern 3: subpath export + second tsup entry" — verbatim exports map and entry array)
    - .planning/phases/03-express-middleware-http-semantics/03-PATTERNS.md (tsup.config.ts + package.json sections — directory-structure preservation note)
  </read_first>
  <action>
    In `tsup.config.ts`, change `entry: ['src/index.ts']` to `entry: ['src/index.ts', 'src/adapters/express/index.ts']`. Leave `format: ['esm']`, `dts: true`, `sourcemap: true`, `clean: true`, `target: 'node24'`, and the `onSuccess` Lua-copy hook UNCHANGED — tsup preserves directory structure, so the adapter entry emits to `dist/adapters/express/index.js` and `dist/adapters/express/index.d.ts`. In `package.json`, add a `"./express"` key to the `exports` map alongside the existing `"."` key, pointing `types` to `./dist/adapters/express/index.d.ts` and `import` to `./dist/adapters/express/index.js`. Do NOT touch the `"."` entry (the core barrel must stay Express-free). NOTE: the build will fail until `src/adapters/express/index.ts` exists (created in plan 03-02), so the build-verify here is deferred to plan 03-03's build-smoke; verify only the config/JSON shape in this plan.
  </action>
  <verify>
    <automated>cd rate-limiter && node -e "const p=require('./package.json'); if(!p.exports['./express'])throw new Error('missing ./express export'); if(p.exports['./express'].import!=='./dist/adapters/express/index.js')throw new Error('wrong import target'); if(p.exports['./express'].types!=='./dist/adapters/express/index.d.ts')throw new Error('wrong types target'); if(p.exports['.'].import!=='./dist/index.js')throw new Error('core export changed');" && grep -q "src/adapters/express/index.ts" tsup.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsup.config.ts` `entry` array contains both `src/index.ts` and `src/adapters/express/index.ts`; the `onSuccess` Lua-copy line is unchanged.
    - `package.json` `exports` has a `"./express"` key with `import: ./dist/adapters/express/index.js` and `types: ./dist/adapters/express/index.d.ts`.
    - The `exports["."]` entry is unchanged (`import: ./dist/index.js`).
    - `grep -q "src/adapters/express/index.ts" tsup.config.ts` succeeds.
  </acceptance_criteria>
  <done>Build is pointed at the adapter entry and the package advertises the `rate-limiter/express` subpath; actual emit is verified by plan 03-03 once the adapter source exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Untrusted third-party package code is installed and will run in the build/test process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (express, @types/express, supertest, @types/supertest) | mitigate | Task 1 is a blocking-human `checkpoint:human-verify` confirming all four [ASSUMED] packages on their registry pages (correct repo, decade-old, no postinstall) before Task 2 installs. Never auto-approvable. |
| T-03-01 | Tampering | package.json exports map | accept | Adding a `./express` subpath only re-points to local `dist/`; no external input crosses here. Low risk. |
</threat_model>

<verification>
- `npm install` of the four devDeps succeeds; `require.resolve('express')` and `require.resolve('supertest')` resolve.
- `package.json` exports both `.` (Express-free core) and `./express` (adapter subpath).
- `tsup.config.ts` has two entries; the Lua `onSuccess` copy is preserved.
- `cd rate-limiter && npm run typecheck` still passes (no new source yet; this confirms the installs did not break the existing type-gate).
</verification>

<success_criteria>
- Four adapter devDeps installed behind a confirmed legitimacy checkpoint; `express` is a peerDependency, not a runtime dependency.
- The build and package metadata expose `rate-limiter/express` as a subpath that will resolve to the built adapter once plan 03-02 creates the source.
- The core entry remains Express-free and unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-express-middleware-http-semantics/03-01-SUMMARY.md` when done.
</output>
