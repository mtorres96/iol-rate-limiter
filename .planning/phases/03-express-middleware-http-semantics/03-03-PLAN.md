---
phase: 03-express-middleware-http-semantics
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - rate-limiter/test/adapters/express/middleware.test.ts
  - rate-limiter/test/adapters/express/fail-open-closed.test.ts
  - rate-limiter/test/build-smoke.test.ts
autonomous: true
requirements: [HTTP-01, HTTP-02, HTTP-03, HTTP-04]
user_setup: []

must_haves:
  truths:
    - "supertest proves a request is admitted then 429'd per key with Retry-After and rate-limit headers"
    - "supertest proves both IETF (`RateLimit`/`RateLimit-Policy`) and legacy (`X-RateLimit-*`) headers appear on allowed AND rejected responses with integer remaining and delta-seconds reset"
    - "supertest proves a throwing-stub limiter under fail-open admits (200) and under fail-closed denies (429) with no unhandled rejection"
    - "supertest proves an empty key admits and logs via the DegradedLogger stub"
    - "the build emits dist/adapters/express/index.js and index.d.ts (subpath ships)"
    - "the full vitest suite and tsc --noEmit pass (build-green gate)"
  artifacts:
    - path: "rate-limiter/test/adapters/express/middleware.test.ts"
      provides: "HTTP-01/02/03 supertest suite (admit→429, headers on both paths, empty-key admit+log)"
      min_lines: 50
    - path: "rate-limiter/test/adapters/express/fail-open-closed.test.ts"
      provides: "HTTP-04 supertest suite via a throwing-stub RateLimiter (fail-open admits, fail-closed denies, no crash)"
      min_lines: 30
    - path: "rate-limiter/test/build-smoke.test.ts"
      provides: "Extended build-smoke asserting the Express subpath lands in dist"
      contains: "adapters/express/index"
  key_links:
    - from: "rate-limiter/test/adapters/express/middleware.test.ts"
      to: "rate-limiter/src/adapters/express/index.ts (rateLimit)"
      via: "import + app.use(rateLimit({ limiter }))"
      pattern: "rateLimit\\("
    - from: "rate-limiter/test/adapters/express/fail-open-closed.test.ts"
      to: "rate-limiter/src/adapters/express/index.ts (rateLimit)"
      via: "throwing-stub RateLimiter injection"
      pattern: "Promise\\.reject"
---

<objective>
Author the supertest suites that prove all four phase requirements against the in-memory
store (no Redis), plus extend the build-smoke test to assert the `rate-limiter/express`
subpath actually lands in `dist`. This is the verification wave — it closes HTTP-01..04
under the mandatory build-green gate (`tsc --noEmit` + full Vitest suite).

Purpose: HTTP-01/02/03 are proven end-to-end via a real `express()` app + supertest with a
deterministic in-memory `TokenBucketLimiter`; HTTP-04 (fail-open/closed) is proven WITHOUT
Redis via a throwing-stub `RateLimiter` (D3-08). The build-smoke extension guards the new
subpath emit that plan 03-01 wired and plan 03-02's source enables.
Output: Two new supertest files under `test/adapters/express/` and an extended
`test/build-smoke.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-express-middleware-http-semantics/03-RESEARCH.md
@.planning/phases/03-express-middleware-http-semantics/03-PATTERNS.md
@rate-limiter/src/types.ts

<interfaces>
<!-- Public surface under test (created in plan 03-02) -->
import { rateLimit } from "../../../src/adapters/express/index.js";   // or from "rate-limiter/express" once built
import type { RateLimitOptions } from "../../../src/adapters/express/index.js";

<!-- Core test fixtures (rate-limiter/src/index.js) -->
import { FakeClock, MemoryStore, TokenBucketLimiter } from "../../../src/index.js";

<!-- RateLimiter contract for the throwing stub (HTTP-04) -->
interface RateLimiter { consume(key: string, cost?: number): Promise<Decision>; }
const boom: RateLimiter = { consume: () => Promise.reject(new Error("store down")) };

<!-- DegradedLogger stub shape (rate-limiter/src/types.ts) -->
interface DegradedLogger { warn(obj: Record<string, unknown>, msg: string): void; }

<!-- Prescriptive header assertions (RESEARCH §"Code Examples") -->
ok.headers["ratelimit"]            // matches /^"default";r=\d+;t=\d+$/
ok.headers["ratelimit-policy"]     // contains '"default";q='
ok.headers["x-ratelimit-remaining"]// integer string
blocked.status === 429
blocked.headers["retry-after"]     // defined (delta-seconds)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTTP-01/02/03 supertest suite (admit→429, headers on both paths, empty-key admit+log)</name>
  <files>rate-limiter/test/adapters/express/middleware.test.ts</files>
  <read_first>
    - rate-limiter/test/token-bucket.test.ts (deterministic Vitest + FakeClock + MemoryStore setup() pattern; import core via the built barrel relative path)
    - rate-limiter/test/degraded.test.ts (logger-stub capture pattern: push `warn(obj,msg)` calls into an array to assert a log fired)
    - rate-limiter/src/adapters/express/middleware.ts (the surface under test — option keys, header behavior)
    - .planning/phases/03-express-middleware-http-semantics/03-RESEARCH.md (§"Code Examples" supertest snippet — verbatim assertion shapes; Pitfall 3 — use an explicit `keyGenerator` to avoid req.ip flakiness)
    - .planning/phases/03-express-middleware-http-semantics/03-PATTERNS.md (*.test.ts section — supertest harness conventions)
  </read_first>
  <action>
    Create `test/adapters/express/middleware.test.ts`. Import `{ describe, it, expect } from "vitest"`, `express from "express"`, `request from "supertest"`, `{ FakeClock, MemoryStore, TokenBucketLimiter } from "../../../src/index.js"`, and `{ rateLimit } from "../../../src/adapters/express/index.js"`. Build a deterministic setup: `new TokenBucketLimiter(new MemoryStore(), { capacity: 1, refillPerInterval: 1, intervalMs: 1000 }, new FakeClock(0))`, an `express()` app with `app.use(rateLimit({ limiter, keyGenerator: () => "k1" }))` (explicit key to avoid req.ip flakiness — Pitfall 3) and a `GET /` returning `res.send("ok")`. Write tests asserting: (HTTP-01/HTTP-03 allowed) first `GET /` → status 200, `headers["ratelimit"]` matches `/^"default";r=\d+;t=\d+$/`, `headers["ratelimit-policy"]` contains `"default";q=1`, `headers["x-ratelimit-remaining"]` is the integer string `"0"`, `headers["x-ratelimit-reset"]` is a small delta-seconds value (NOT epoch-scale — assert it is `< 1e6`). (HTTP-02/HTTP-03 rejected) second `GET /` → status 429, `headers["retry-after"]` defined, AND the rate-limit headers (`ratelimit`, `x-ratelimit-remaining`) STILL present on the 429 (headers on rejected responses, D3-04), body JSON has `error` and `retryAfterMs`. (D3-03 empty key) a second app with `keyGenerator: () => ""` and a captured `logger` stub → `GET /` returns 200 and the logger captured one `warn` call. (headers modes) one app with `headers: "legacy"` asserts `ratelimit` header absent but `x-ratelimit-limit` present; one with `headers: false` asserts neither present.
  </action>
  <verify>
    <automated>cd rate-limiter && npx vitest run test/adapters/express/middleware.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - supertest: first GET 200 with `RateLimit`, `RateLimit-Policy`, and `X-RateLimit-*` headers (integer remaining, delta-seconds reset < 1e6).
    - supertest: over-limit GET returns 429 with `Retry-After` AND rate-limit headers still present + JSON body with `error`/`retryAfterMs`.
    - supertest: empty key returns 200 and the `DegradedLogger` stub captured a `warn`.
    - supertest: `headers: "legacy"` omits `RateLimit`; `headers: false` omits all rate-limit headers.
    - `npx vitest run test/adapters/express/middleware.test.ts` passes.
  </acceptance_criteria>
  <done>HTTP-01/02/03 proven end-to-end against the in-memory store via supertest.</done>
</task>

<task type="auto">
  <name>Task 2: HTTP-04 fail-open/closed supertest suite via a throwing-stub RateLimiter</name>
  <files>rate-limiter/test/adapters/express/fail-open-closed.test.ts</files>
  <read_first>
    - rate-limiter/test/degraded.test.ts (rejecting-stub injection + logger-capture pattern for the error path)
    - rate-limiter/src/types.ts (RateLimiter contract for the stub)
    - rate-limiter/src/adapters/express/middleware.ts (the catch/policy branch under test)
    - .planning/phases/03-express-middleware-http-semantics/03-RESEARCH.md (§"Code Examples" HTTP-04 snippet — fail-open admits, fail-closed denies; D3-08)
  </read_first>
  <action>
    Create `test/adapters/express/fail-open-closed.test.ts`. Import vitest, `express`, `supertest`, `{ rateLimit }` from the adapter barrel, and `type { RateLimiter }` from `../../../src/types.js`. Define `const boom: RateLimiter = { consume: () => Promise.reject(new Error("store down")) }`. Test (fail-open default): app with `rateLimit({ limiter: boom, keyGenerator: () => "k" })` (no policy → default fail-open) + `GET /` route → `request(app).get("/")` returns 200 (admitted, request never crashes). Test (explicit fail-open): same with `policy: "fail-open"` → 200. Test (fail-closed): `rateLimit({ limiter: boom, keyGenerator: () => "k", policy: "fail-closed" })` → returns 429 (denied), no unhandled rejection. Test (catch logs): pass a captured `logger` stub and assert the catch path captured one `warn` on the rejection (D3-08). Optionally add a process-level `unhandledRejection` guard within the suite to assert none fired. Use a deterministic `keyGenerator` so the error path — not key extraction — is what is exercised.
  </action>
  <verify>
    <automated>cd rate-limiter && npx vitest run test/adapters/express/fail-open-closed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - supertest: throwing-stub limiter under default (fail-open) → 200.
    - supertest: throwing-stub under `policy: "fail-closed"` → 429.
    - supertest: the catch path captured a `warn` via the `DegradedLogger` stub.
    - No unhandled rejection / process crash during the suite.
    - `npx vitest run test/adapters/express/fail-open-closed.test.ts` passes.
  </acceptance_criteria>
  <done>HTTP-04 proven without Redis: the middleware-owned fail-open/closed policy handles a rejecting limiter, no crash, logged.</done>
</task>

<task type="auto">
  <name>Task 3: Extend build-smoke to assert the Express subpath lands in dist + run the build-green gate</name>
  <files>rate-limiter/test/build-smoke.test.ts</files>
  <read_first>
    - rate-limiter/test/build-smoke.test.ts (existing: runs `npm run build` in beforeAll, asserts the Lua assets land in dist via `fileURLToPath(new URL(...))` + `readFileSync`)
    - rate-limiter/tsup.config.ts (the second entry wired in plan 03-01 — emits `dist/adapters/express/index.js` and `index.d.ts`)
    - .planning/phases/03-express-middleware-http-semantics/03-PATTERNS.md (build-smoke section — extend the same pattern for the subpath)
  </read_first>
  <action>
    Extend the existing `test/build-smoke.test.ts` (do NOT remove the Lua-asset assertions). Reusing the same `beforeAll` that runs the real `npm run build`, add a `describe`/`it` asserting that `dist/adapters/express/index.js` and `dist/adapters/express/index.d.ts` both exist and are non-empty (`readFileSync(fileURLToPath(new URL("../dist/adapters/express/index.js", import.meta.url)), "utf8").length > 0` and the same for `index.d.ts`). This proves plan 03-01's second tsup entry + the `./express` export resolve to real built files. Keep the existing 120s build timeout. After authoring, run the full build-green gate.
  </action>
  <verify>
    <automated>cd rate-limiter && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `build-smoke.test.ts` still asserts the three Lua assets AND now asserts `dist/adapters/express/index.js` + `index.d.ts` exist and are non-empty.
    - `npm run typecheck` (`tsc --noEmit`) passes.
    - `npm test` (full Vitest suite, including the two new supertest suites) passes — the mandatory build-green gate.
  </acceptance_criteria>
  <done>The subpath emit is guarded and the entire suite + typecheck pass green — phase verification gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supertest client → Express app under test | Synthetic requests drive the middleware's allow/deny/error paths in-process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Denial of Service | unhandled rejection from a throwing limiter (HTTP-04) | mitigate | Task 2 asserts the throwing-stub path returns a response (200 fail-open / 429 fail-closed) with no process crash and no unhandled rejection — this IS the test that proves T-03-04's mitigation from plan 03-02. |
| T-03-07 | Tampering | build emits a stale/empty subpath that ships a broken adapter | mitigate | Task 3 runs the real `npm run build` and asserts the subpath JS + d.ts are non-empty, catching a broken second-entry wiring before it ships. |
</threat_model>

<verification>
- `cd rate-limiter && npm run typecheck` passes.
- `cd rate-limiter && npm test` passes — the full suite including the two new supertest suites and the extended build-smoke (this is the mandatory build-green gate at the phase boundary).
- The two new supertest suites prove HTTP-01..04 against the in-memory store with no Redis dependency.
</verification>

<success_criteria>
- HTTP-01/02/03 verified end-to-end via supertest (admit→429, headers on both paths, integer remaining, delta-seconds reset, empty-key admit+log).
- HTTP-04 verified via a throwing-stub limiter (fail-open admits, fail-closed denies, no crash, logged).
- The `rate-limiter/express` subpath provably builds (dist assets non-empty).
- `tsc --noEmit` + full Vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/03-express-middleware-http-semantics/03-03-SUMMARY.md` when done.
</output>
