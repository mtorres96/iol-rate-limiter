---
phase: quick-260625-qz8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rate-limiter/docker-compose.yml
  - rate-limiter/README.md
autonomous: true
requirements: [QZ8-01, QZ8-02]

must_haves:
  truths:
    - "A shell `RL_LIMIT=2 docker compose up` actually overrides the limiter limit (interpolation, not literal)."
    - "With no env vars set, `docker compose config` still resolves RL_ALGO=token-bucket, RL_LIMIT=5, RL_WINDOW_MS=60000, RL_REFILL=5."
    - "`docker compose config --quiet` exits 0 (the interpolated file is still valid)."
    - "README documents three distinct override paths: compose-with-Redis interpolation, one-off compose run, and standalone no-Redis docker run."
    - "README states which paths use real Redis (a/b) vs in-memory store (c)."
    - "`npm run verify` stays green (no TS source touched)."
  artifacts:
    - path: "rate-limiter/docker-compose.yml"
      provides: "app.environment with the four RL tunables as Compose interpolation"
      contains: "RL_LIMIT: \"${RL_LIMIT:-5}\""
    - path: "rate-limiter/README.md"
      provides: "Three documented run-with-different-config commands near the env-var table"
      contains: "docker compose run --rm --service-ports"
  key_links:
    - from: "rate-limiter/docker-compose.yml app.environment"
      to: "shell environment"
      via: "Compose ${VAR:-default} interpolation"
      pattern: "\\$\\{RL_LIMIT:-5\\}"
---

<objective>
Make the four demo rate-limiter tunables in `docker-compose.yml` shell-overridable via
Compose interpolation (same defaults preserved), and document the three docker config-override
run commands in the README.

Purpose: Today the `app.environment` values are literals, so `RL_LIMIT=2 docker compose up`
silently does NOT override them — the demo always runs at 5/60000. Switching to
`${VAR:-default}` interpolation lets reviewers tune the limiter from the shell without editing
files, while unset behavior is byte-for-byte unchanged.

Output: Interpolated `docker-compose.yml` (four tunables only), expanded README run examples.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@rate-limiter/docker-compose.yml
@rate-limiter/README.md

<interfaces>
<!-- Current literal app.environment block (docker-compose.yml lines 18-24). -->
<!-- REDIS_URL and PORT MUST stay literal; only the four RL_* tunables become interpolated. -->
    environment:
      REDIS_URL: redis://redis:6379   # KEEP LITERAL — compose-network address
      PORT: "3000"                     # KEEP LITERAL — tied to "3000:3000" ports mapping
      RL_ALGO: token-bucket
      RL_LIMIT: "5"
      RL_WINDOW_MS: "60000"
      RL_REFILL: "5"

<!-- Demo reads all of these from process.env with code-side defaults: -->
<!--   DEFAULT_LIMIT=5, DEFAULT_WINDOW_MS=60000, RL_REFILL defaults to limit, RL_ALGO defaults to token-bucket. -->
<!-- README env-var table lives at lines 91-98; existing override example at lines 100-109. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Interpolate the four RL tunables in docker-compose.yml</name>
  <files>rate-limiter/docker-compose.yml</files>
  <action>
In the `app.environment:` block, replace ONLY the four rate-limiter tunables with Compose
interpolation, keeping the SAME defaults so unset behavior is unchanged:
  - `RL_ALGO: "${RL_ALGO:-token-bucket}"`
  - `RL_LIMIT: "${RL_LIMIT:-5}"`
  - `RL_WINDOW_MS: "${RL_WINDOW_MS:-60000}"`
  - `RL_REFILL: "${RL_REFILL:-5}"`
Leave `REDIS_URL: redis://redis:6379` and `PORT: "3000"` as LITERALS — do NOT interpolate them:
REDIS_URL must stay the compose-network address, and PORT is tied to the literal `"3000:3000"`
ports mapping. Update each of the four trailing comments to note the value is now
shell-overridable with the shown default (e.g. `# Tunable: allowed budget; shell-overridable,
default 5.`). Do not touch redis service, healthcheck, ports, depends_on, build, or init.
  </action>
  <verify>
    <automated>cd rate-limiter && docker compose config --quiet && docker compose config | grep -E 'RL_ALGO: token-bucket|RL_LIMIT: "?5"?|RL_WINDOW_MS: "?60000"?|RL_REFILL: "?5"?' | wc -l | grep -q 4</automated>
  </verify>
  <done>`docker compose config --quiet` exits 0; with no env set the four resolve to token-bucket / 5 / 60000 / 5; REDIS_URL and PORT remain literal; a shell `RL_LIMIT=2 docker compose config` would resolve RL_LIMIT to 2.</done>
</task>

<task type="auto">
  <name>Task 2: Document the three config-override run commands in README</name>
  <files>rate-limiter/README.md</files>
  <action>
In the Configuration section, near the existing env-var table (lines ~91-109), expand the
override examples into THREE clearly-labeled ways to run with a different limiter config,
keeping the existing env-var table intact. Replace/augment the current single
`docker run -e RL_LIMIT=10 ...` example with:
  (a) With Redis (compose), via shell interpolation — `RL_LIMIT=2 RL_WINDOW_MS=4000 docker compose up`
      (note: unset falls back to defaults 5 / 60000).
  (b) With Redis, one-off — `docker compose run --rm --service-ports -e RL_LIMIT=2 -e RL_WINDOW_MS=4000 app`.
  (c) Standalone image, NO Redis (in-memory store) — `docker run --rm -p 3000:3000 -e RL_LIMIT=2 rate-limiter-app`.
State explicitly that (a) and (b) exercise the real distributed Redis path, and (c) uses the
in-memory store (unset REDIS_URL). Keep the surrounding prose accurate — the line at ~108-109
about Compose setting REDIS_URL/RL_ALGO/tunable defaults should remain correct after the edit.
Do NOT modify DESIGN.md (it does not document these run commands — confirmed via grep).
  </action>
  <verify>
    <automated>cd rate-limiter && grep -q 'RL_LIMIT=2 RL_WINDOW_MS=4000 docker compose up' README.md && grep -q 'docker compose run --rm --service-ports' README.md && grep -q 'docker run --rm -p 3000:3000 -e RL_LIMIT=2 rate-limiter-app' README.md && grep -qi 'in-memory store' README.md</automated>
  </verify>
  <done>README documents all three run paths near the env-var table; (a)/(b) labeled as real Redis, (c) as in-memory store; env-var table preserved; DESIGN.md untouched.</done>
</task>

</tasks>

<verification>
- `cd rate-limiter && docker compose config --quiet` exits 0.
- With no shell env set, `docker compose config` resolves the four tunables to token-bucket / 5 / 60000 / 5; REDIS_URL and PORT stay literal.
- README shows the three labeled run commands with Redis-vs-memory distinction.
- `cd rate-limiter && npm run verify` stays green (no TS source changed; gate unweakened).
</verification>

<success_criteria>
- The four RL_* tunables are interpolated `${VAR:-default}`; a shell override now takes effect under `docker compose up`.
- Unset behavior is byte-for-byte identical to before (5 / 60000 / token-bucket / refill 5).
- README documents compose-interpolation, one-off compose run, and standalone no-Redis run, with the Redis vs in-memory store distinction made explicit.
- `npm run verify` passes unchanged.
</success_criteria>

<output>
Create `.planning/quick/260625-qz8-make-docker-compose-rl-env-vars-shell-ov/260625-qz8-SUMMARY.md` when done.
</output>
